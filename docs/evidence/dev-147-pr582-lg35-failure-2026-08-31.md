# DEV-147 — PR582 LG 35 failure and power check, 2026-08-31

Scope: the same candidate boot as the [LG 27 startup snapshot](dev-147-pr582-candidate-startup-2026-08-31.md), after David's requested monitor comparison. The earlier startup record remains unchanged.

## Result

David reports that the LG 35 also does not recognize an attached device. He calls this a regression and raises loss of USB-C power as a concern. Both reported video attempts failed. This is worse than the earlier working behavior, but the causal change is not yet isolated. Same-cable use and the exact physical sequence remain unconfirmed.

A bounded unprivileged snapshot at 01:05:33 CEST confirms:

| Check | Observed result |
|---|---|
| Boot and target module notes | Same as the candidate startup snapshot |
| External DP-1 | Disconnected, disabled, no modes |
| Internal eDP-1 | Connected/enabled; compositor active at 2560×1664 |
| Lower/front USB-C supply, `0-003f` | `online=1`, USB type `PD` selected |
| MagSafe supply, `0-003a` | `online=1`, USB type `PD` selected |
| Battery | 100%, Full; current 0 |

USB-PD is reported online. This does not establish which source supplies current while MagSafe is attached and the battery is full. It does not prove sustained monitor-only charging, but it does not support declaring total USB-C power loss from the monitor's no-device message. Keep MagSafe connected; no unplug-for-charging test follows.

## Saved evidence

Both journal and compositor reads exited 0 with empty stderr. The journal has 1,207 valid same-boot records, 902,196 bytes and 1,207 unique cursors. Its first 970 parsed records exactly match the startup snapshot; 237 records were added. Before/after boot IDs match both each other and the earlier capture. Raw logs stay private. Journal SHA-256: `7ed1e5f28f5aeb97a6b213e5e3daf4319e16125f3b38dc25f2c629508f0fe4fa`.

T1 reaches its explicit diagnostic cap: sequences 1–128 are contiguous, ending in `cap`, `reason=budget`. Before the cap, no cached HPD-high state or connected-HPD begin is present. Cache entries show plug/USB-capability changes and both orientations. These are controller-reported transitions, not an exact count or attribution of David's cable actions. Later T1 silence is not evidence that later events did not occur.

The journal records one `cd321x_interrupt: failed to read events` at approximately 373.8 seconds. This is a USB-C-controller investigation lead, not a demonstrated cause. External DCP disconnect calls appear, but no relevant clear-swap timeout or explicit coprocessor crash was found in this saved interval. Neither the timeout fix nor the current crash-guard value is validated.

## Stop and next boundary

Stop further swaps, mode changes and candidate experiments. Preserve both snapshots, all images and earlier successes/failures. No privileged command, live probe, driver operation, boot-file or runtime change was made by the agent. Reads used the same bounded display/journal commands as the startup check, plus fixed standard power-supply status attributes. The unsafe partner `usb_mode` attribute was not read.

Recommend a separately approved attended return to the previously working W image, `initramfs-linux-asahi-dpalt.img`, to seek restored service. This is a recovery proposal, not authorization for a reboot or a guarantee of video recovery. Keep MagSafe, lid open, saved work and the recovery guide available. The matched PR582 control is not W and has not booted; causal comparison can wait. W/default retain the prototype DTB, so neither is full stock rollback.

Open: same-cable confirmation, recovery approval/result, cause of low HPD/controller error, matched-control evidence, sustained power, USB data and reliability. No upstream submission follows this failed case.
