# DEV-147 — fixed T1 capture, 2026-08-30

## Scope and current limit

This record covers the minimum collector and boot/artifact binding for the accepted T1 image. The accepted image, parser, staging helper and 54-test staging result are reused. No old suite or image build was repeated.

The bounded collector and fixed-T1 binding are implemented. The sole focused GREEN passed all 13 tests. Independent retained-result QA and source/executed-boundary/export review passed without a rerun. The live entry remains held and unrun. This is not a complete or sealed A3/A4 package. No live capture, staging, sudo, boot, cable, device or recovery action is authorized or recorded.

## Retained semantic RED

Independent source review preceded the fresh exact-input probe `run-x98n3klr` and the sole RED `run-gv3ttd_p`. Raw records and frozen inputs remain private.

| Check | Observed result |
|---|---|
| Fresh isolation probe | Exit 0; containment PASS and seven standard-library smoke tests PASS |
| New collector/binding tests | Three tests in 0.001 seconds; three intended assertions; zero errors or skips; exit 1 |
| Inputs | 588 read-only bindings; identical probe/workload inputs; unchanged after execution |
| Outputs | Four harness files plus `setup.json` and `after.json`; zero fixture children or capture/image outputs |
| Independent result QA | PASS without a rerun |

The two new functions deliberately returned `None`. Assertions independently detected missing child execution, missing active size-limit termination/reaping, and missing fixed-T1 binding. These are missing-feature RED results, not a working collector or a hardware failure. Two runner inventory assumptions were corrected before execution. Result-QA assumptions about mount ordering and harness temporary files were corrected during read-only audit; they caused no workload failure.

Exact inner command:

```text
/usr/bin/python3.14 -I -S -B /inputs/tests/run_fixed_capture_tests.py
```

The unchanged private launcher is SHA-256 `3544e55bd504019344b6358e1829686759abecc5f9f4c8534901290df963851e`. The accepted v6 manifest is `6f999183c660b49c3ba665a9bc9b22d316beca81cf01a9b4a403f5c1435a9391`. No runtime entry was added or repinned.

The frozen RED runner is `4b3d33aab90eb18cec8c45d161b09698f89c9eea2ead7f111e5c336067b8c5a1`; its three-test oracle is `e7e207267c18f3789a9895050d2d30f9eec6dcd6b00155bed71c12762c48d942`. The shared input record is `3a2227d5d746e67760cf067b92e02d14abd3a214843950cb6e808d0e894f8409`. Setup and after records match.

## Focused implementation and GREEN

Fresh exact-input probe `run-_q_e70wu` preceded the sole GREEN `run-fve4cazd`. Both used the unchanged launcher and v6 runtime above, with the fixed inner command. Neither the accepted image nor an old suite was rebuilt or rerun.

| Check | Observed result |
|---|---|
| Fresh isolation probe | Exit 0; containment PASS and seven standard-library smoke tests PASS |
| Collector/binding tests | 13 methods in 1.632 seconds; zero failures, errors or skips; exit 0 |
| Inputs | 588 read-only bindings; identical probe/workload inputs; unchanged after execution; no timeout |
| Actual fixture children | 19 valid receipts and two deliberate incomplete-output cases, kept separate |
| Independent result QA | PASS on retained raw files, pins, metadata, bounds and receipts; no rerun |
| Source, executed boundary and public export review | PASS; no live-entry or manual-handoff release |

The original three RED tests are unchanged. Ten added methods cover active stream limits, clean process policy, timeouts, descendants, signals, write failures, collisions, same-inode tampering, sample boundaries, provisional publication, identity refusals and incomplete traces. These added methods are GREEN regression coverage, not separately recorded failing-before-fix RED results.

Pre-execution review corrected signal/process cleanup, partial-open handling and readback integrity defects. The sole GREEN passed; there was no failed GREEN workload. Its frozen runner is `c8e7c183e053f6f155783471f43050ca9752f30996b7ba1bb33befea6536abce`. The shared input record is `efada8ae3390d81d20d46bdb4d55db123122b969f95ae909d95f0c84f69250e0`; setup and after records match. Workload stderr is `2e15fb4f15699fe6cf0cc85781d0321d72a2955429d200f4e3516b6462d02ac5`.

All 19 valid receipts name the Python command actually executed. Raw bytes, hashes, private modes and bounds match; each direct child is recorded reaped. Observed exits are nine zero, nine SIGKILL and one exit 7. Expected timeout/error/interruption cases are not capture success. The two deliberate incomplete cases retain collision or altered bytes and lack valid numeric-exit receipts. Their passing tests assert PID disappearance and descriptor cleanup; do not count them as observed exit-zero results.

The fixed live wrapper is implemented but was never invoked. Tests exercised its internal synthetic-file seams and refusal to relabel a Python child as journalctl. Synthetic result publication remains provisional and requires an independently observed successful outer invocation plus raw-record audit. No journal, live boot/module sample, staging result or image was produced.

## Source saved for review

The [source archive](../../dev/apple-dp-altmode/usbdiag/tipd-capture/README.md) contains eight byte-identical exports and one wrapper with exactly one invalid `LIVE_OUTPUT` literal. The public wrapper is `a23d7566530ab9a519730c08d7e609108b76c3e9e449870fceeda923b1cc3eee`; its unchanged path-hash guard refuses before live access. The runner retains the private wrapper pin. Private staging-prefix bytes and the runtime manifest are excluded, so the public checkout is not a runnable capture package.

## Evidence limits

The staging oracle combines retained producer output, not a newly executed helper. Its 45-line prefix stays private. It does not establish real staging.

Staging output and initrd selection remain user attestations. Matching capture bytes, boot IDs and module notes establish consistency; they do not independently prove which initrd booted or the earliest module load. Fixture child receipts must name the Python command actually executed, never journalctl.

A structurally complete log prefix can omit a whole closed suffix. It cannot prove absence of a later sender call. A sender call/return does not prove receiver delivery or video, USB or charging reliability.

The [main plan](../plans/dev-147-m2-displayport.md#minimum-remaining-path-living) owns the remaining path. The [diagnostic subplan](../plans/dev-147-usb-startup-diagnostic.md#remaining-collector-exit-criteria-living) owns the focused exit criteria. Full-suite QA remains UNRUN/HOLD because the historical suite attempted a real-home credentials-path write. No-install exceptions remain; no new framework or dependency is introduced.
