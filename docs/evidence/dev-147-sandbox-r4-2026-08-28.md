# DEV-147 sandbox R4 evidence — 2026-08-28

> Public archival copy, prepared 2026-08-28. Actual boot IDs are redacted where present. Local evidence links are marked as private. Commit references identify the retained private branch unless they name upstream source. Recorded hashes and past QA results describe the private originals, not this edited export. Commands below are historical records, not instructions to run the public helpers.

**Host / scope:** Personal-owned omarchy-air; one offline sandbox correction and one QA run.
**Approval:** David said “yes, go ahead!” to correcting the namespace-denial check and running one additional isolated QA round. This was the explicit exception to the earlier three-round stop. No driver build, image work, staging, or boot was included in this run.
**Repository:** `codex/dev-147-m2-dp-altmode`, starting at `6646dc00bf1e8f107062dd8f47165910eccb7261`.

## Result

The fourth sandbox QA round passed. It clears the specific isolation-check hold, not D1 as a whole. No diagnostic module or image was built. No hardware action ran. The [diagnostic subplan](../plans/dev-147-usb-startup-diagnostic.md) owns remaining D1 work; the [main plan](../plans/dev-147-m2-displayport.md) owns acceptance and rollback.

The new private R4 checkpoint (retained privately) retains the correction, approval, review, run, and integrity checks. The earlier [D1 hold evidence](dev-147-usbdiag-d1-hold-2026-08-28.md) and its private 60-file checkpoint remain unchanged. This new run cannot recover the third run's missing return value or errno.

## Correction and review

Only three private code files changed. The launcher points to the new private directory; its namespace, mount, environment, descriptor, and seccomp policy did not change. The probe now declares `unshare(int) -> int`, clears errno, captures the return and errno immediately, and flushes their JSON record before validation. The smoke suite adds six tests of the real, pure denial predicate.

The predicate accepts return `-1` with `EPERM` or `ENOSPC` only for `CLONE_NEWUSER`, and return `-1` with `EPERM` only for `CLONE_NEWNS`. It rejects successful calls, unexpected returns/errors, and unknown or combined flags. The [Linux manual](https://man7.org/linux/man-pages/man2/unshare.2.html) documents these error meanings. The installed-version [bubblewrap source](https://raw.githubusercontent.com/containers/bubblewrap/v0.12.0/bubblewrap.c) implements the private namespace-count limit. No protection was removed to make the assertion pass.

Independent static review passed before QA. The tool manifest, test launcher, and proof input stayed byte-identical. The frozen third-run failure remains the observed RED; no extra baseline run was made. The new fixtures first ran in the single approved fourth round. No Pydantic, pytest, Ruff, or strict type-check tooling was installed or claimed as passed; the retained standard-library/manual-validation/unittest exception applies to these private tools.

## Executed check

The QA agent ran exactly once, with no retry:

```bash
timeout --signal=TERM --kill-after=5s 290s /usr/bin/python3 -I -S -B /home/david/o/.dev147-stage/usbdiag-r4-20260828.7zi4bldt82/sandbox-tools/test_launcher.py
```

The wrapper exited 0. run-34jkngdh (retained privately) records `exit_code: 0`, `inputs_unchanged: true`, and `timed_out: false`. Stderr is empty. The actual namespace results are `CLONE_NEWUSER` / flag `268435456` / return `-1` / errno `28` (`ENOSPC`), then `CLONE_NEWNS` / flag `131072` / return `-1` / errno `1` (`EPERM`). Both attempts were denied.

All seven unittest methods passed: the original standard-library smoke and six predicate tests covering 32 cases. The full probe also passed UID/GID, environment, null-only devices, private outputs, descriptor scan through 524287, zero core limit, forbidden-path absence, privilege/capability/keyring checks, absent IPv4/IPv6 routes, read-only root/input canaries, private write canaries, and tool-version checks. There was one bubblewrap invocation, with 585 read-only binds, two private writable binds, and no post-probe workload. All 585 mounted input fingerprints matched before and after.

The parent read back the result and logs. All 15 readable system/prototype/recovery hashes and kernel/package versions matched before and after. The old 60-file checkpoint verified again. Protected stock initramfs/GRUB and staged-image contents were not freshly read with privilege. qa-result.json (retained privately) and the raw run files retain the checks.

## Limits and next boundary

This verifies the defined probe on this host, not universal sandbox security or protection against kernel flaws and host resource exhaustion. New jobs must keep the same reviewed boundary, fresh private outputs, pinned read-only inputs, bounded execution, and no unrestricted fallback. The trace/image helpers remain unimplemented; their suites, pahole authentication, control builds, diagnostic builds, ABI checks, and archive/index controls remain pending. No firmware, startup-USB, reliability, or rollback acceptance gate was closed.

Resume the already approved offline D1 implementation and private build work next. Keep its evidence separate from this R4 run. D2 staging and D3 attended boot still need separate review and user action. The monitor is not needed for offline work; no actual unplug was confirmed or counted as a test.

## Rollback

Leave the private correction unused. No live change needs reversal. Keep both checkpoints, the working DP image/core, old helpers, both timestamped boot backups, and the macOS recovery bundle. Do not run this frozen checkpoint's launcher again; a later job needs a fresh continuation directory and review. Normal boot still uses the stock driver image with the earlier candidate DTB; R4 did not perform full rollback.
