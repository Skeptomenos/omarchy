# W/LG27 identified startup — 2026-08-31

Result: one user-identified W startup passes display and monitor-USB enumeration checks. It is not a reliability pass or a validated kernel repair.

## Physical report and identity

David reports the exact GRUB line `initrd /boot/initramfs-linux-asahi-dpalt.img`, external and internal images, and a responsive system. The intended case uses LG 27UN83A-W on the established front/lower path. No intervening cable action was reported; the agent did not independently observe physical insertion or pixels.

The 11:16:31 CEST capture has matching before/after boot IDs, different from the pre-reboot checkpoint. Kernel/package versions remain `7.1.6-1-1-ARCH` / `linux-asahi 7.1.6.asahi1-1`; mkinitcpio is 41.1-1 and m1n1 is 1.6.1-1.

Loaded build IDs:

- AppleDRM: `dd5e291114047bb4d7c83a529cddb4f4ac9292d7` — packaged working module.
- TIPD: `8fd9e3d39ee211f439471a812fb5eaa2622f7585` — working patched core.

These notes corroborate the user-selected W case. They do not authenticate every loaded image byte or retroactively identify earlier ambiguous boots.

## Captured result

| Area | Observation |
|---|---|
| Internal | eDP-1 connected/enabled, 2560×1664 at 60 Hz |
| External | DP-1 connected/enabled, 16 advertised mode entries, active 3840×2160 at 59.997 Hz |
| Compositor | Both outputs enabled and DPMS on |
| Monitor USB | `1-1 0bda:5411` USB2.1 hub; `1-1.3 043e:9a39` LG Monitor Controls |
| USB roots | `1d6b:0002` and `1d6b:0003`; no downstream mouse test yet |
| Power | MagSafe controller `0-003a` and monitor controller `0-003f` both PD online; battery Full/100%, current_now 0 |
| Partner mapping | Controller `0-003f` maps to `port2`, with a partner; no partner `usb_mode` read |

PD online with a full battery does not measure charging rate or sustained monitor-only charging. USB2 enumeration does not establish USB3 throughput.

## Startup sequence

The task's execution trace records one current-boot kernel-journal command at all priorities, with a 20-second deadline and 8 MiB file limit. It returned zero, with empty stderr: 849,945 bytes, 1,151 unique cursor records. Monotonic and realtime ordering pass. All records match the captured boot. Coverage runs from 1.893384 s to 341.208027 s after boot; no later event is inferred. Independent saved-data review verifies these contents, not the original acquisition command or completeness of the kernel's logging.

| Time after boot | Event |
|---|---|
| 3.710 s | xHCI bus 1 registers on controller `0x502280000` |
| 4.389 s | External DCP receives `dcp_dptx_connect(port=0)` |
| 4.763 s | Monitor hub `0bda:5411` enumerates |
| 5.146 s | LG controls `043e:9a39` enumerate |
| 6.603 s | External display HPD asserted |
| 6.672 s | External connector publishes 16 mode entries |
| 6.939 s | First 4K mode request completes |
| 15.027 s | Last captured startup modeset completes at 4K |

This establishes early enumeration in this boot without relying on a post-login reconnect. Ordinary startup also requests a temporary 2560×1440 mode before the final 4K mode. The agent issued no mode commands. This does not identify a greeter-focus cause.

The external DCP repeats four clock-frequency errors, three `load_ca_data` diagnostics and three PMU errors. Five initial crossbar `-517` deferrals precede successful binding. No captured DCP crash, poweroff timeout, persistent atomic rejection, AFK exhaustion, FIFO error, kernel BUG/panic, DART fault or USB enumeration error was found. This is a bounded negative finding, not proof of an error-free system. The journal also retains other platform, wireless and audit warnings; they were not erased or reclassified as display failures.

## Scope, preservation and checks

Raw journal, USB/display details, boot IDs and Linear history remain private. Journal SHA-256:

`5430a897b347de057472ee237d1234c38b926bf89eb420167102114e5807724e`

All 12 capture commands exited zero with empty stderr. A saved-file manifest authenticates their bytes. The saved-data gate and synthetic invalid-input checks are defined in the [continuation plan](../plans/2026-08-31-dev147-w-startup-checkpoint.md). The first validator attempt compared hyphenated and compact boot-ID formats literally and refused valid data. The corrected comparison removes hyphens; wrong-boot and duplicate-cursor fixtures still fail separately. No raw capture was modified.

No sudo, reboot, reconnect, device insertion, tracing, driver/image build, staging, system configuration change or upstream submission occurred during this capture. The one-use W startup handoff is consumed. Existing images, backups, default selection and the prototype DTB remain unchanged; W is not full stock rollback.

The acquisition limits and absence of runtime mutations above are statements about this task's executed commands. No independent whole-system before/after audit was taken. Saved-file validation cannot prove those execution facts or physical setup by itself.

Independent saved-data QA and semantic review PASS. The verifier reran the complete gate, rejected separate wrong-boot and duplicate-cursor streams, and independently checked USB-before-HPD timing, startup modesets and diagnostic counts. No disputed claim remains after the explicit provenance qualification. This review did not perform a live read or test.

## Consequence

Earlier LG35 and W root-only startup records remain true. This LG27 case shows successful attached startup is possible with the existing W image; it does not prove that intermittent USB initialization is repaired or identify why this case differs. Do not rebuild just to chase the previous absent-hub snapshot.

The next useful manual case is one wired mouse through the monitor's USB-A port, followed by David's visible movement/click confirmation and a bounded device/log check. Keep USB-C and MagSafe unchanged. Repeated startup, hotplug, cold-start, suspend, cable compatibility, sustained charging and rollback remain unvalidated. Timer-Off testing stays parked.
