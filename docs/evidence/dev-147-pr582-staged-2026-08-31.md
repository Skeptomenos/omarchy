# DEV-147 — PR582 pair staged, 2026-08-31

Scope: David ran the [reviewed staging handoff](dev-147-pr582-staging-preparation-2026-08-31.md), supplied its complete success receipt, and asked to reboot.

## Accepted result

The user-run receipt contains both `PAIR STAGING PASS` and `Staging exit: 0`. It names the intended control and candidate image paths and a retained root-private checks directory. This consumes the staging handoff.

Independent read-only metadata agrees:

| Image | Bytes | Owner / mode / links |
|---|---:|---|
| `/boot/initramfs-linux-asahi-dpalt-pr582-control.img` | 21,621,100 | root:root / 0600 / 1 |
| `/boot/initramfs-linux-asahi-dpalt-pr582-candidate.img` | 21,621,939 | root:root / 0600 / 1 |

Both are regular files. The retained checks directory is root:root, mode 0700. The running kernel remains `7.1.6-1-1-ARCH`; all seven bound package versions match. Wrapper SHA-256 `0df4e987d43000bbf8e4aa1a2189a7621310a070d048b2c25cec089cbb8e3d8a` and library SHA-256 `6b20d119791f4322e101a92b9e5b850ba3098d35dbf966f2d7918cb3918694f9` remain unchanged.

The protected image hashes and pre/post invariants are evidenced by the successful user-run validator. The agent did not independently reread root-only image contents, GRUB or retained logs. Metadata alone is not a checksum proof. Independent receipt/handoff review accepts this qualified staging PASS; no extra privileged read or completed test replay is required.

## One attended candidate boot

David's request advances to one conditional, user-run candidate boot. The private boot handoff preserves the default entry and changes only the existing initrd filename in the GRUB editor to `initramfs-linux-asahi-dpalt-pr582-candidate.img`. The expected AppleDRM build ID is `62b12eeb40983345a587cb46a168662817efe54a`.

David must remain present after startup. Keep a healthy internal display, responsive Linux, lid open, MagSafe, battery above 50%, saved work, recovery instructions on another device and unchanged cables. If these conditions do not hold, defer the boot. Unexpected menu/path means Esc and stop; no blind selection or Terminal `initrd` command.

After startup, obtain the physical screen and responsiveness report, then verify the loaded module. A blank external screen does not authorize reconnecting or retrying. Internal-display failure or unresponsiveness is a safety stop. Startup alone cannot prove the timeout fix; the timeout/recovery and matched-control comparison remain open.

## Preservation

No agent executed sudo, reboot, a live diagnostic or driver operation. No reboot or hardware result is claimed by this record. Default startup, existing images, DTB and recovery backups remain protected by the accepted staging checks. Leaving the new images unselected abandons this test without deleting them; it does not restore the original DTB.

Keep the staging receipt, checks directory, [exported crash-flag capture](dev-147-crashflag-export-2026-08-31.md) and prior history. The [main plan](../plans/dev-147-m2-displayport.md#minimum-remaining-path-living) owns current state and the later test result.
