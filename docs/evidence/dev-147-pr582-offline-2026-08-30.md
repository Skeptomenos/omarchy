# DEV-147 — PR #582 offline preparation, 2026-08-30

Scope: paired private AppleDRM builds and image preparation. This is not a hardware test or a staging release.

## Source and build

The full 40-file Apple DRM subtree at `e2e1930a9595bffafad92cec2b5504525efb9cd4` was acquired: 330,384 bytes. All Git blob IDs and byte sizes match. The connected GitHub API supplied the files after raw-file requests hit HTTP 429. Five retained files were reused. The upstream no-final-newline layout of unused `dcp_trace.c` was preserved.

Only the timeout hunk from [PR #582 commit 6b70d02b](https://github.com/AsahiLinux/linux/commit/6b70d02bcb5758a625d8bcedbff340cf544a4496) changed. Francisco Vargas retains patch credit and Signed-off-by. The parent commit's unrelated plane-order change was not imported.

Independent plan and pre-execution build review passed. The unchanged v6 sandbox used the accepted private headers, configuration, symbols, vmlinux, pahole and libraries. No package was installed. Each run passed its fresh isolation probe and seven standard smoke checks.

| Run | Result |
|---|---|
| `run-fr4re6c3` | 14 focused image-contract tests PASS; exit 0, inputs unchanged, no timeout. |
| `run-_nr9en89` | Unmodified AppleDRM build; exit 0, inputs unchanged, no timeout; build and outer stderr empty. |
| `run-x_gtk4vf` | Patched AppleDRM build; exit 0, inputs unchanged, no timeout; build and outer stderr empty. |

Both builds used the native Makefile and the same virtual `/work/apple` path. It selected all 17 driver objects, including audio, AV, trace and both firmware variants. There are also three Kbuild-generated/aggregate objects. Both modules retain 270 imports and zero exports, matching the packaged driver. Name, vermagic, dependencies and aliases match stock. Their `apple_dcp` DWARF and BTF layout dumps match. BTF uses the module's extracted `.BTF.base`.

| Variant | Bytes | SHA-256 | GNU build ID |
|---|---:|---|---|
| Control | 8,744,352 | `c8fffa9a663760cb3c2f66f8d9123c76f01a6a5dfc51744ece1d36af1e54f7c3` | `8bc7a79d757fc70fbfae14ee050fc7c2353387ad` |
| Candidate | 8,744,856 | `a05be348a400e2283d75990fc45efd097883d226e2becbd758854eb850c787f4` | `62b12eeb40983345a587cb46a168662817efe54a` |

Only `iomfb_v12_3.o`, `iomfb_v13_3.o` and aggregate `appledrm.o` differ. The other 17 objects are byte-identical, including `dcp.o`, `plane.o` and `apple_drv.o`. Source diff returns the exact seven-to-24-line hunk and no other source change. The rebuilt modules retain debug data, so their bytes are not expected to equal the packaged module.

Independent binary review passed. In the control, the timeout stores to byte 64 occur at `0x108a8` (v12.3) and `0x166a8` (v13.3). The candidate instead calls the warning at `0x109d4` and `0x167d4`, then returns through the existing epilogue. Both 50 ms waits remain. Saved RTKit-handler and atomic-guard disassembly are byte-identical between builds.

## Private images

The sole assembly run, `run-0gclt3ml`, completed with exit 0, unchanged inputs, no timeout and empty outer stderr. Its 13 bounded child commands succeeded. The early archive and all 1,162 other main records remain byte-identical. All seven indexes remain unchanged. Each image changes only AppleDRM and passes full readback plus binary-only, no-load dependency lookup. The closure is `typec → drm_dma_helper → appledrm`; no new depmod run was needed.

| Private image | Bytes | SHA-256 |
|---|---:|---|
| `initramfs-linux-asahi-dpalt-pr582-control.img` | 21,621,100 | `32e61a249223e88b9aace49ac23f03f49870ff63d0f102e82d801efa097f1764` |
| `initramfs-linux-asahi-dpalt-pr582-candidate.img` | 21,621,939 | `7ad2ba90f8a2986227beefdb53c3b9b1c06deeee66b0e709a9eda0fa1074669c` |

The bound private image entry differs from the tested archival source only by the module-manifest hash. All reused helper hashes match. Independent result QA passed: all 13 command triplets, module/build-ID and image hashes, all 583 runtime input rows, fresh isolation and unchanged input results agree. Neither image is staged or boot-tested.

## Verification limits

The small archive wrapper and its tests were drafted together. This is an explicit exception to the pre-implementation RED sequence; no such RED is claimed. The tests exercise real parser/replacer code with archive fixtures, not hardware. The existing no-install exception uses stdlib unittest and strict manual parsing. Ruff, mypy and the unrelated Omarchy aggregate suites were not run. The upstream kernel patch retains native logging and formatting. Git whitespace checks pass for authored files. The byte-exact upstream patch triggers expected warnings for unified-diff context prefixes and its final context line; its hash is preserved instead of reformatting it.

The [observed guard](dev-147-crashflag-observed-2026-08-30.md) remains a recovery blocker, not proof of its writer or the initial video-loss cause. The patch does not clear this boot's guard. It keeps the 50 ms wait and immediate timeout return, so later poweroff steps remain skipped. Genuine firmware-crash handling must remain intact.

All old source, images, boot files and recovery backups remain unchanged. The existing root-private diagnostic bundle is volatile and must be exported before a planned reboot. The [main plan](../plans/dev-147-m2-displayport.md#pr-582-offline-preparation-living) owns the next attended boundary. No staging, sudo, live driver action, cable/device change, mode change, suspend, reboot, recovery rehearsal or upstream submission ran.
