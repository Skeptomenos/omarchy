# W/LG35 attached startup — 2026-09-01

Result: DEV-147 Case D PASS with an execution-sequence qualification. The accepted W image started with the LG 35 ultrawide attached. David reports images on the internal and external displays and a responsive system. The external output runs at the monitor's native 3440×1440 mode at 99.982 Hz.

## Sequence and identity

A separate boot-cleanup task required one coordinated reboot before the planned LG27-to-LG35 live switch. David kept the physical setup, selected the exact W image by changing only the `initrd` basename, and completed the reboot. That task reports a healthy internal display, external display, Wi-Fi, and responsive Linux. The 3440×1440 EDID mode and LG HDR WQHD identity identify the attached external display as the tested LG 35 ultrawide.

The selected line was:

```text
initrd /boot/initramfs-linux-asahi-dpalt.img
```

Boot ID: `261ba5db-68fc-4044-8cd8-09687a5fcba3`.

The command line contains one persistent `disablehooks=encrypt` argument from the separate boot-cleanup work. It does not select or modify the display image, kernel, DTB, or display modules.

| Area | Observation |
|---|---|
| Kernel | `7.1.6-1-1-ARCH` |
| TIPD | `8fd9e3d39ee211f439471a812fb5eaa2622f7585`, matching the accepted patched core |
| AppleDRM | `dd5e291114047bb4d7c83a529cddb4f4ac9292d7`, matching the packaged working driver |
| DCP device tree | Internal and external DCP nodes both report `okay` |
| Internal | eDP-1 connected and enabled at 2560×1664/60 Hz; DPMS on |
| External | LG HDR WQHD DP-1 connected and enabled at 3440×1440/99.982 Hz; DPMS on; 14 advertised modes |
| Layout | External starts at compositor x=1600 and does not mirror eDP-1 |
| Power | MagSafe and monitor power controllers online; aggregate AC online; battery Full/100% |
| System | Zero failed systemd units and zero active systemd jobs at capture |

## Startup messages

The current boot records the external hotplug callback at 6.760 seconds and a 3440×1440/99 Hz modeset request at 6.788 seconds. The final recorded native modeset completes at 14.095 seconds. The known EDT frequency, CAHandler data-version, and PMU diagnostics recur. A bounded scan finds no kernel BUG/panic/Oops, DART/IOMMU fault, RTKit/coprocessor crash, or AFK exhaustion.

At a second read-only check 460 seconds after boot, DP-1 remains active at 3440×1440/99.982 Hz with DPMS on.

## Scope and qualification

The exact W filename and physical images are user provenance relayed through the coordinated boot task. Boot identity, command line, module notes, DCP status, DRM/compositor modes, power state, systemd state, and bounded kernel messages are same-boot software observations.

This boot superseded the planned same-boot LG27-to-LG35 cable switch. It proves W compatibility with the second tested monitor at native mode. It does not add a second hot-switch result. Case C already supplies one controlled hot-reconnect result on W, so repeating a physical switch would add risk without closing the remaining acceptance gap.

No DEV-147 reboot, cable action, mode command, module change, boot-file write, suspend, USB-data test, or upstream action occurred during this evidence capture. The remaining Slice 1 action is one suspend/resume classification test, performed last.

Independent fresh-context verification PASS. It rechecked the live boot identity, module IDs, DCP nodes, display modes, power, systemd state, event timing, fatal-pattern scan, local links, and diff hygiene. Exact W selection, physical pixels, responsiveness, physical setup, and no-mutation history remain explicit user or coordinated-task provenance.
