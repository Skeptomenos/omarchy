# DEV-147 — one-boot USB startup diagnostic design

Current boundary, reconciled 2026-08-31 after [W/LG27 identified startup](../evidence/dev-147-w-lg27-startup-2026-08-31.md): the exact user-selected W case has both native outputs and monitor hub/LG controls enumerated during startup. Both PD supplies are online. This supersedes only the last root-only snapshot, not earlier failures or their unresolved cause. No new diagnostic build is justified by this successful case. The [continuation plan](2026-08-31-dev147-w-startup-checkpoint.md) owns saved-data verification and the next proposed single wired-mouse test. This subplan retains historical diagnostic design, not authority to replay it. The startup handoff is consumed; no image/runtime change or upstream submission occurred. Timer-Off testing stays parked and the watch paused.

Completed offline checkpoint, 2026-08-30: the [T1 manual-boundary package](../evidence/dev-147-t1-manual-package-2026-08-30.md) passed independent handoff, manifest, safety and checksum review. Its seal, image, 54-method staging result and 13-method collector/binding result remain unchanged. That offline checkpoint ran no hardware; the separate [attended result](../evidence/dev-147-t1-boot-capture-2026-08-30.md) owns the later boot and capture.

Updated: 2026-08-30, with [A0 findings](../research/dev-147-hpd-startup-2026-08-29.md), [T1 selected after A1 review](#a1-selection--t1-tipd-sender-diagnostic-living), and the first [fresh E no-change control PASS](../evidence/dev-147-e-operational-third-attempt-pass-2026-08-30.md). This subplan owns its technical gates. The later one-use T1 case is consumed; all further device and greeter tests remain held. The [latest display recovery](../evidence/dev-147-w-recovery-after-e-2026-08-28.md) retains its filename qualification: shared W/E IDs do not independently prove W startup. The [confirmed E failure](../evidence/dev-147-c4-selection-confirmation-2026-08-28.md), D3 external-display FAIL / measurement INCONCLUSIVE, and [C3 staging](../evidence/dev-147-usbearly-staging-2026-08-28.md) remain separate; all handoffs are consumed. Root-private provenance stays qualified. Firmware questions and USB/full Gate 4b remain HOLD; causality is unknown. The main plan owns the user-attested USB-C-only charging report, not a USB-data or controlled-power result. The login-focus issue is outside this goal.

Current offline implementation branch: `codex/dev-147-t1-image-offline`. The public archive branch remains `codex/dev-147-m2-dp-altmode-public`; its first checkpoint `c781312d1` was pushed and verified by remote readback. The [public archive](../../dev/apple-dp-altmode/usbdiag/README.md) preserves the authored drafts and fixtures, but excludes host manifests and raw logs. Older public boot/staging helpers have invalid machine-identity/path placeholders. New C2 workload copies preserve exact source bytes but still require private sandbox inputs; none may run live. The original operational helpers and historical private branch remain unchanged. The [dated source checkpoint](../evidence/dev-147-public-source-checkpoint-2026-08-28.md) distinguishes historical R4 evidence from the new private RED runs.

The [main plan](dev-147-m2-displayport.md) owns overall acceptance, the [consumed recovery handoff](dev-147-m2-displayport.md#current-recovery-handoff--previous-working-dp-image-living), and the [next approval boundary](dev-147-m2-displayport.md#next-step--offline-correction-design-only-living). This subplan owns the diagnostic and correction design. The [dated investigation](../research/dev-147-usb-startup-2026-08-28.md) owns earlier source findings; the [D3 failure record](../evidence/dev-147-usbdiag-startup-failure-2026-08-28.md) owns the failed measurements and logging-defect proof. The [recovery record](../evidence/dev-147-dp-recovery-2026-08-28.md) owns the later successful display recovery, not a diagnostic fix. The [correction-design record](../evidence/dev-147-usbdiag-correction-design-2026-08-28.md) retains C0's read-only checks and reviews; the new [C1 record](../evidence/dev-147-usbdiag-c1-correction-2026-08-28.md) owns executed source/test evidence. Full Gate 4b remains on hold.

## Question and limit

The question below is the retained D0–C2 software-order question. The distinct [T1 selection](#a1-selection--t1-tipd-sender-diagnostic-living) asks which TIPD sender decisions precede its HPD call; it does not claim receiver delivery.

Does this controller's first initialization call the USB2 mode API with a missing handle, then reach the actual ATC HOST setter only after DWC3 core initialization? Record failed attempts and automatic retries too. A later successful attempt can still report `PROBE_PENDING` after an earlier attempt acquired a handle.

The design intended to establish software call order. D3 did not measure it because both start markers and all later diagnostic records are absent. Even a valid trace could not read whether the PHY latched a setting or prove why the monitor hub did not enumerate. Logging and earlier module availability can change timing. A newly working startup would not be a fix or a reliability pass.

## Progress (LIVING)

| Gate | State | Exit |
|---|---|---|
| D0 — design and independent review | Initial review complete; semantic gap found in D3 | The historical measurement/image/rollback review passed, but did not catch the OF target-name mismatch. |
| D1 — offline implementation and build | Complete; historical offline QA PASS | 59 trace, 58 archive, and 55 assembly tests pass. Private builds, import/logging checks, no-change controls, and the 413-command image verification pass. These checks do not validate the target predicate or hardware behavior. |
| D2 — stage a distinct image | Complete; user-run staging PASS | David's final helper PASS validates image and protected post-checks. All 40 reported pin records match; independent metadata agrees. Root-private contents were not reread. No reboot implied. |
| D3 — one attended diagnostic startup | External-display FAIL; measurement INCONCLUSIVE | Intended loaded IDs match. Internal screen normal; external connector disconnected/disabled. Zero markers in 963 records; target-name guard defect proved. Handoff consumed; no retry. |
| Recovery after D3 | One functional display PASS; handoff consumed | Both native displays and responsiveness restored. Packaged USB drivers and the working patched DP core verified. Only root hubs enumerate; USB acceptance HOLD. Not full DTB rollback or a diagnostic fix. |
| Correction C0 | Complete; retained design | Exact guard, production-path regression coverage, controls, and separate review gates remain the contract. C0 itself added no executed test or hardware result. |
| Correction C1 | Focused QA and independent source/test review PASS | Two-target generation-zero RED precedes correction. V2 passes 10 target methods / 54 synthetic-FDT executions, 65 trace tests, and format/cap checks. Source only; no module/image or live test. |
| Preparation C2 | Offline artifact checks, independent review, and seal PASS | Fresh controls, exact v2 modules, and only E are verified. Retained REDs precede 3 metadata, 13 E, and 15 verifier GREEN methods; actual E assembly passes 414 commands. The 2,270-file checkpoint stays sealed. No load/stage/boot result. |
| C3 — E-only staging | Complete; user-run staging PASS | All 41 visible initial-preflight rows match. David's final report validates the later root-private checks; independent readable hashes and metadata agree. E is staged; no replay. |
| C4 — E case | External-display FAIL; E selection confirmed | User confirmation and saved boot capture agree. Cause unknown; handoff consumed and no retry. |
| Recovery after E | Functional display recovery observed; handoff consumed | Fresh boot and both native outputs after intended W recovery; filename not restated and shared IDs not image proof. Root hubs only; USB HOLD. |
| A2 — private T1 image | Private assembly and independent result QA PASS | The [sole assembly `run-mvqmtbw_`](../evidence/dev-147-t1-private-image-2026-08-30.md) followed pre-execution review and fresh probe `run-vpqm7rk3`. Both exited 0 without timeout or input change, with 594 read-only mounts. One private image has exactly one TIPD payload change and seven unchanged indexes. Six offline children and independent read-only result QA passed. [Source/fixture RED/GREEN](../evidence/dev-147-t1-assembly-preexecution-2026-08-30.md), accepted E proof and both failed E attempts remain history. Later staging and the attended T1 result are separate; assembly alone did not establish startup or hardware acceptance. |
| Autonomous offline goal / A3–A4 and user staging | Package sealed; staging PASS; one-case release consumed | The [manual package](../evidence/dev-147-t1-manual-package-2026-08-30.md) and [staging receipt](../evidence/dev-147-t1-user-staging-2026-08-30.md) remain unchanged. The later [attended result](../evidence/dev-147-t1-boot-capture-2026-08-30.md) is separate; no replay. |
| T1 — attended startup and fixed capture | New-monitor startup FAIL; qualified identity/capture PASS | The [correction](../evidence/dev-147-t1-monitor-identity-correction-2026-08-30.md) attributes failure to the new LG 27/cable. Expected kernel/T1 note, consistent binding and saved-file QA remain valid. Thirty-nine records include two connected-HPD returns. The later LG 27 RCA row corroborates original-monitor hotplug success, not startup acceptance. Cause remains open; the case is consumed. |
| LG 27 investigation and user cable change | Transient 4K60 PASS; later recurrence below | The [before/after result](../research/dev-147-lg27-link-investigation-2026-08-30.md) preserves new-monitor failure and verified video after David's cable change, with unchanged boot/T1 identity. Accurate monitor metadata, exact physical setup and reliability remain open. No new driver patch or live action follows. |
| LG 27 spontaneous loss and failed recovery | External video FAIL; scoped evidence QA PASS | The [recurrence](../evidence/dev-147-lg27-spontaneous-dropout-2026-08-30.md) records connected/disabled DP-1, 16 modes, rejected atomic requests and AFK allocation exhaustion. Internal screen and system stay normal. Poweroff latch is the leading recovery hypothesis, not proven; initial loss remains unexplained. No live action is released. |

D1 included source implementation, focused fixtures, the two module builds, and private image preparation under David's explicit approval. It did not include installation, staging, a driver reload, or a reboot. David then approved D2 helper preparation. Its offline implementation and tests are complete in a fresh private continuation. David has now completed the reviewed production preflight and privileged staging. The agent did not execute either. Each later action stops for its own review and user action.

The [D2 helper record](../evidence/dev-147-usbdiag-staging-helper-2026-08-28.md) owns the retained RED runs, environment-loop and EXIT-trap defects, narrow corrections, and final independent 38-test PASS. The [public helper](../../dev/apple-dp-altmode/stage-usbdiag-initramfs.sh) has deliberately invalid constants. David used the final pinned private copy. The [D2 staging record](../evidence/dev-147-usbdiag-staging-2026-08-28.md) records the complete user PASS and independent metadata. Pending staging instructions are superseded; do not execute either the completed final command or the earlier private candidate again.

The [helper-QA record](../evidence/dev-147-offline-helper-qa-2026-08-28.md) owns initial image-helper GREEN, import/logging verification, the retained userspace linker failure, and saved-gzip validation. The later [real-control record](../evidence/dev-147-real-archive-controls-2026-08-28.md) owns the real format failure, narrow correction, 58 tests, scratch-output stop, and successful no-change archive/index control. None is a module-load or boot-safety result.

The [private-image record](../evidence/dev-147-private-diagnostic-image-2026-08-28.md) owns the later symbol-index and alias-normalization stops, their independent investigation, test-first corrections, 55 passing assembly methods, and the verified candidate. It also records that the first 30 assembly fixtures were written after their draft implementation. Do not describe those original fixtures as test-first evidence.

The [R4 evidence](../evidence/dev-147-sandbox-r4-2026-08-28.md) records the approved correction and one successful full probe, including namespace, network, write-boundary, and seven focused test checks. The [D1 hold record](../evidence/dev-147-usbdiag-d1-hold-2026-08-28.md) retains the earlier three failures; its missing errno remains unknown. The tool manifest and protection policy did not change. The [trace/build checkpoint](../evidence/dev-147-trace-and-module-builds-2026-08-28.md) records later GREEN tests, the independent review failures and correction, pahole signature/executable authentication, private builds, and remaining checks. No package was installed; builds used the verified private header tree.

The unchanged private D1 archive retains its drafts and failed attempts. The private R4 checkpoint retains the corrected sandbox and successful run. Neither frozen checkpoint was reused for outputs. A fresh private continuation passed the R4-equivalent isolation probe, then ran the trace and image stubs: 32 trace tests reported 40 NotImplementedError errors; 16 image tests reported 30. Both runs preserved all inputs and did not time out. These are genuine RED results, not implementation passes. At that checkpoint, fifteen readable system/prototype/recovery pins matched, and the earlier 60-file D1 and 34-file R4 seals verified. No root-only image/GRUB read or live driver action occurred in D1. Permission to unplug during offline work was not a confirmed action or hardware-test result. The C1 offline scope also permits monitor disconnection, but does not confirm it or authorize a diagnostic reconnect.

## Target and source boundary

Use the existing J413/T8112 machine and kernel `7.1.6-1-1-ARCH`. Keep the current candidate DTB and working DP core. Source remains pinned to Asahi `e2e1930a9595bffafad92cec2b5504525efb9cd4`.

| Layer | Diagnostic target | Basis |
|---|---|---|
| Connector | Front/lower left USB-C, `0-003f` | Existing prototype route |
| DWC3 glue | `/soc/usb@502280000` | Saved `dwc3_1` node |
| ATC PHY | `/soc/phy@503000000` | Saved `atcphy1`; both USB PHY references point here |

The retained preprocessed DT (retained privately) supplies these links. Filter diagnostic output by J413 compatibility and the exact OF node. Print only fixed labels such as `front_lower`, `dwc3`, and `atc`. Do not use changing `portN` or PHY instance numbers to identify the monitor. The module still serves other devices; diagnostic filtering does not isolate kernel execution or make the experiment risk-free.

D3 exposed a defect in that implementation: `of_node_full_name()` returns the leaf name for these FDT-created nodes, not the absolute paths above. Both frozen v1 guards return generation zero and suppress every marker. The [pinned-source proof](../evidence/dev-147-usbdiag-startup-failure-2026-08-28.md#confirmed-logging-defect) also records matching frozen and compiled sources. C1 corrects current v2 source only. The requirements below remain the contract, not a claim that D3 met it or that v2 has run in the kernel.

The following two-file restriction remains the historical D0–C2 contract. The approved [T1 selection](#a1-selection--t1-tipd-sender-diagnostic-living) supersedes it only for T1; it does not change or reopen the archived pair.

Only two source files may gain diagnostics: `drivers/usb/dwc3/dwc3-apple.c` and `drivers/phy/apple/atc.c`. Do not change TIPD, the HPD patch, generic PHY/USB/HCD code, DT, DCP/DRM, exported interfaces, or driver control flow. No early PHY acquisition, new error handling, reordered hardware calls, extra hardware reads/writes, new driver locks, retries, delays, or work items. The C1 correction below is a narrow exception to the original "no new locks" wording: the standard OF metadata lookup/refcount API uses its existing tree locking once per eligible probe. Static source review passed; kernel timing and concurrency remain untested. This is not permission for a new driver lock or hardware call. Preserve all pre-existing hardware operations, error/return behavior, timeouts, and driver lock order.

## Required records

Record at existing operation boundaries, never inside register polling or delay loops. Preserve original error returns and no-op branches. Function locations are in the pinned source archive (retained privately).

| Component / site | Required observation |
|---|---|
| DWC3 probe | Target probe generation, begin, ready or original failure. |
| DWC3 role callback | Requested role, existing state, and same-role skip, under its existing lock. |
| `dwc3_apple_init` | Attempt ID and state; early USB2 call begin/end and return; reset result; core-init begin/end; host/gadget-init begin/end; original exit result. |
| ATC probe/finalize | Generation and begin before initialization operations; ready or original failure. |
| ATC mux | Current/target mode, cached lane orientation and pipe-handler state, no-op or configure result. |
| ATC USB2 power-on/off | Begin before first existing write; end after last existing write. These mark execution, not measured electrical state. |
| ATC USB2 mode setter | Entry after its existing lock; requested mode/submode; exit and unchanged return, including HOST_SS rejection and HOST fallback. |

At the early USB2 call and core-init return, record `usb2_present` and `usb2_error` as separate booleans. A non-null error pointer is not a usable handle. Do not dereference or print that pointer, add a validity guard, or introduce a WARN. Keep the early setter unmoved and preserve its currently ignored return semantics.

Use a module-wide sequence that survives probe retries, a component-local probe generation, and a DWC3 initialization-attempt ID within each generation. ATC and DWC3 generation numbers are independent. Failed probes and deferred attempts must remain visible. Do not join the streams by matching generation numbers or treat the first successful attempt as the first attempt.

Use native `pr_info()` / KERN_INFO with one fixed, newline-terminated JSON payload per record. Include schema version, a fixed diagnostic revision token, fixed component/target, sequence/generation/attempt where applicable, event/phase, and only the cached values needed above. The archived initial token is `dev147-usbdiag1-v1`; corrected source uses only `dev147-usbdiag2-v1` as specified below. Bind each to its own reviewed source/patch manifest and, after a separately approved build, both binary hashes/build IDs. No v2 binary identity exists from C1. Both earliest probe markers must carry the expected token. A schema version or a later loaded build ID alone cannot identify the code that handled the first probe. Retain boot identity, priority, message, cursor, and original timestamps in the journal envelope. No application logging library, raw pointers, MMIO values, register dumps, serial numbers, arbitrary strings, or backtraces. An absent field must be explicit or specified by the event schema; never substitute a plausible value.

Limit each component to 128 records per module load, including one reserved `capture_capped` marker, with at most 384 bytes per record. Saturate the counter and stop diagnostics after the cap; driver execution must continue unchanged. The implementation must be race-safe across target probe retries without changing the existing lock order. Do not use rate-limited logging that silently drops selected calls. No dynamic-debug, tracefs, BPF, console-level, or kernel-command-line changes.

The [kernel logging guidance](https://docs.kernel.org/core-api/printk-basics.html) warns that logging can delay execution. The pinned header exposes `printk_deferred`, but the retained `Module.symvers` does not export `_printk_deferred`; do not add an export or use that route. The [format guidance](https://www.kernel.org/doc/html/latest/core-api/printk-formats.html) also requires correct integer formats. Native kernel logging is the explicit exception to the application-logger recommendation in the logging skill; JSON records and levels remain required.

## Image containment and timing change

The known working image is the private Gate 4 image (retained privately), SHA-256 `ae8f1ed7f4f258f89931209cd7de6030be9f6875372d7329151b822a6ba2281f`, 19,184,103 bytes. Its root-private staged copy is `/boot/initramfs-linux-asahi-dpalt.img`. Preserve both unchanged. The private source is available for offline work; no extra privileged read is needed during design.

The saved working archive contains 199 modules, including `phy-apple-atc`, `dwc3`, `udc-core`, and the xHCI modules. It does not contain `dwc3-apple`. Its direct dependency closure is already present. The verified diagnostic image replaces ATC, adds DWC3 glue, and replaces only `modules.dep.bin` and `modules.alias.bin`. It has 200 modules. The original symbol index, both builtin indexes, and DP core remain byte-identical.

The private candidate is 19,647,739 bytes, SHA-256 `a11bf3a2bac1f105aa57b08ce9fad338c68882851247f7524d09f4b7c94188ca`. It has seven unchanged early records and 1,163 main records. All 1,159 unrelated original main records and the archive tail are unchanged. All 200 binary-only filename/dependency checks and both target OF aliases pass. The original 199 dependency results are identical. David staged this candidate at `/boot/initramfs-linux-asahi-dpalt-usbdiag1.img` and selected it for D3. That startup failed the external-display criterion. Retain the failed image unchanged; do not select it again.

Adding the glue permits earlier automatic probing, even without an explicit preload. Record this as a separate timing variable. This boot cannot be compared with the old image as a timing-matched A/B test. If a later causal comparison is needed, its control must match this packaging and logging policy; that is not part of D3.

Verified preparation route: preserve raw records from the saved archive and write a new private image. Generate indexes in a separate reduced module root. The saved image has 199 dependency records and seven indexes; the full private tree has 1,861 dependency records. The image lacks `modules.order`, `modules.builtin`, and `modules.builtin.modinfo`. Use those exact pinned inputs only in the generation root; do not run bare `depmod` over an extracted archive. Keep these inputs and extra generated outputs out of the image. The generated symbol binary changes only ordering priorities; its exact bytes and complete mapping dump are pinned. Retain the original symbol binary in the final index set. All five static indexes must remain byte-identical. Only the dependency and alias binary indexes may change, with full final binary-only resolution checked.

The unmodified control now passes index regeneration, lossless raw-record serialization, and gzip reconstruction. Independent GNU cpio and bsdtar listings agree; complete reconstructed image bytes are identical. No general archive extraction was needed. The saved image has seven early members, 1,162 main members, and gzip starting at byte 10,240. Preserve that split, record placement, payloads, type, permissions, UID/GID, link targets, and hardlink relationships in the diagnostic image. Independent stdout-only extraction of its selected module payloads must also match. No archive-member path may escape a private directory. Keep embedded `buildconfig` unchanged as base-image provenance; retain new build/repack provenance externally.

The private operational core-only preparation/staging helpers are fixed to the completed experiment and must stay unchanged. Their public archival copies have deliberate identity redactions and are not live-use helpers. Do not rerun, parameterize, or weaken the operational guards. A new bounded helper needs its own tests and review. It must reject an existing or symlinked output, private-tree hardlinks to system files, source/image drift, an active package transaction, and missing build isolation. It must never fall back to an unrestricted build. Keep all incomplete artifacts for inspection; no cleanup or automatic retry.

D1 requires a real unprivileged build sandbox with only a fresh persistent build directory and its temporary area writable. Source snapshots, exact headers/`Module.symvers`, toolchain, and required inputs are read-only. No access to credentials, network during compilation, host package writes, or live device interfaces. The current unrestricted execution tool is not itself a build sandbox. If containment cannot be established, stop and report it. Each command has the existing five-minute limit; retain logs and ask before retrying a timeout.

Before release for D2, require all of these checks:

- Exact kernel/config/header/toolchain provenance, module ABI/imports/dependencies/aliases, distinct diagnostic build IDs, and no new unreviewed warnings. Build and inspect unmodified source as an offline control if needed to resolve build-method differences; do not load it.
- The working image/source, candidate DP core, all original stage artifacts, old gate scripts, and backups remain unchanged. Recheck actual host drift before execution; a dated design check is not a fresh preflight.
- The new archive retains all 199 original module paths, replaces ATC, and adds only DWC3 glue: 200 modules total. Only reviewed non-builtin index differences may accompany that delta; no extra depmod inputs or outputs enter the archive. Both builtin indexes remain identical. All other original payloads, metadata, link relationships, and archive placement match, including DP core, DT-related content, firmware, `init`, hooks, keyboard/disk unlock support, configuration, OpenSSL, and symlinks. No new preload, custom boot hook, persistent entry, or runtime service.
- Independent extraction and dependency/index resolution show that the intended diagnostic modules are available for the first probe. A later loaded build ID alone does not prove which code handled the first event. D3 requires their earliest probe markers and diagnostic identities too.
- The new image's exact hash, size, member/delta manifest, and source/patch/build records are pinned. David created the fixed destination `/boot/initramfs-linux-asahi-dpalt-usbdiag1.img` through the reviewed helper. The pre-staging absence guard passed in that run. Its presence forbids rerunning or overwriting. The later D3 selection and failure are a separate result.

D2 helper preparation is complete. The new reviewed, fixed-source/fixed-destination staging-only helper preserves atomic no-replace publication, repeated protected hashes, permissions, sync, and incomplete-result rules. It protects the working DP image and both installed USB modules. It rechecks source-image hash/size, kernel and package versions, package-transaction state, free space, recovery copies, and protected stock/working files before any copy. Public source retains invalid host identifiers. The final private copy completed staging; retain it without rerunning it. Preserve the old helper and all frozen D1/D2 preparation outputs.

The 38 focused failure-path and real-tool tests pass in isolation with synthetic boot/source files. They cover existing destinations and links, source/protected-file drift, synthetic host/kernel/package records, package lock, space validation, bounded copy failure, interruption, and final success-marker ordering. A missing-path sync error checks propagation, not storage-device failure. Independent QA and safety review pass. Never run the helper against live `/boot` for QA. Preserve partial results on failure; do not add an overwrite or automatic cleanup option. David's complete final PASS now supplies the user-run result. Its root-private check directory is `/boot/.dev147-usbdiag-stage.ESqzIgLr8I`; the agent checked only directory/file metadata. The paste has no separate numeric exit-status capture. Do not repeat a completed gate or treat staging success as permission to reboot.

## D3 — attended diagnostic case

The case is complete with external-display FAIL and measurement INCONCLUSIVE. The [dated result](../evidence/dev-147-usbdiag-startup-failure-2026-08-28.md) records its output/power/taint observations, loaded IDs, capture checks and limits, and the target-name defect. Preserve the failed capture. The separate [working-image recovery](../evidence/dev-147-dp-recovery-2026-08-28.md) has since restored both displays; it does not change D3's result. Do not retry either handoff, reconnect the monitor, add a USB device, change modes, suspend, or use a live module swap.

<a id="current-d3-handoff--one-user-selected-restart"></a>

### Historical D3 handoff — consumed; do not repeat

The steps and case rules below are retained as history, not current authority. At readiness review, David confirmed no other active work, both physical screens normal, MagSafe and the front/lower USB-C monitor connected, and no device attached to the monitor's USB ports. The [readiness record](../evidence/dev-147-usbdiag-boot-readiness-2026-08-28.md) preserves those checks. The handoff permitted one attended diagnostic selection with unchanged cables, orientation, HDMI/input settings, and lid position. It was consumed by the failed startup.

1. When ready to leave this Linux session, run only `sudo reboot` in the desktop Terminal.
2. At the visible GRUB menu, press an arrow key to stop the countdown. Highlight `Arch Linux`, then press `e`. Do not send blind key presses through other boot screens.
3. Find the existing `initrd` line. Replace only the filename `initramfs-linux-asahi.img` with `initramfs-linux-asahi-dpalt-usbdiag1.img`. Keep the existing `/boot/` prefix and any other entry contents. The expected line is shown below; it is not a Terminal command.
4. Leave the `linux /boot/vmlinuz-linux-asahi ...` line and every kernel argument unchanged. Press Ctrl-x once to boot the edited entry. Esc cancels the edit. These keys follow the [GRUB menu-editor reference](https://www.gnu.org/software/grub/manual/grub/html_node/Menu-entry-editor.html).
5. After login, report whether each physical screen shows an image. Keep all cables, input settings, modes, and lid position unchanged. Allow one 30-second observation before the fixed-boot log capture described below. Do not reconnect, retry, change modes, or suspend.

```text
initrd /boot/initramfs-linux-asahi-dpalt-usbdiag1.img
```

If the menu, expected stock filename, or path differs, press Esc and stop this case. If startup passes the menu and reaches login without the edit, report that missed selection; do not start another test boot or use a Terminal `initrd` command. A failed test has only the reviewed recovery route below, not an automatic retry. If a safety-stop event occurs, stop immediately; do not wait 30 seconds just to collect more data.

The edit changes only this boot's in-memory entry. A normal unedited boot selects the stock driver image but leaves the candidate DTB installed and can lose external video. It is not full rollback. Keep the working DP image and both timestamped backups. Before restarting, make the existing private macOS recovery guide available without relying on Linux; no Recovery visit or rehearsal is required now. Its restore execution remains untested.

Save work. Confirm healthy internal display, matching kernel/packages, intact backups/recovery bundle, battery strictly above 50%, and David's attendance. Recheck that no downstream storage or other new USB device was added to the monitor. If its contents are unknown, hold; software absence does not clear that physical check.

Keep the lid open, same front/lower cable/port/orientation, MagSafe, HDMI, and monitor input settings. No mouse or storage test. Use one user-selected restart with the separately staged diagnostic image and unchanged kernel arguments. The GRUB menu-editor procedure applies only after D2 and readiness review. That handoff must name the newly verified diagnostic image; the old reference names the working DP image instead. No desktop `initrd` command and no blind menu input. This is a restart, not a cold-start test.

The historical case called for physical screen reports and one fixed boot-ID, all-priority kernel-journal window through a declared end after a 30-second observation. External-image loss instead triggered the stop condition; the responsive machine's evidence was captured without deliberately extending the test. Original fields/cursors were preserved and envelope checks passed. This does not establish measurement completeness or recover the missing diagnostic events. Only reviewed identity/build-ID, USB enumeration, DRM/compositor, and power attributes were in scope. Never read partner `usb_mode`, sweep arbitrary sysfs files, or change tracing/logging settings to fill a missing capture.

Require the actual monitor hub `0bda:5411` and LG controls `043e:9a39` under the target USB controller for an enumeration success. Root hubs alone do not pass. Record display modes and both power sources, without claiming that a full battery proves isolated USB-C charging. Preserve full firmware diagnostics and the new boot's taint baseline; do not carry the old boot's taint value forward as a fresh result.

There is no physical reconnect, second boot, automatic correction, mode change, suspend, or live module swap in this case. Missing USB enumeration with otherwise healthy screens is the diagnostic question, not permission to retry. A new internal-screen failure, unexpected persistent external loss, charging regression, WARN/BUG/panic, coprocessor/IOMMU fault, or repeated timeout stops the case. Save evidence only if the machine remains responsive, then use the reviewed recovery route.

## Interpretation and acceptance

Actual D3 outcome: envelope validation passed, but measurement completeness did not. Matching loaded IDs and correct bound targets do not replace the missing first-probe markers. The source-level guard defect explains why records were suppressed; it does not establish the video-failure cause or the intended USB call order.

Assess capture validity separately from hardware behavior. Require both component start markers, verified diagnostic identities/target mapping, consecutive sequences, all earlier probe/initialization attempts, closed critical operation pairs, and a known collection boundary. Missing starts/tail, gaps, duplicate/restarted sequences, a cap marker, unmatched operations, or only a successful suffix make the capture inconclusive. A closed attempt with an explicit error is useful failure evidence, not a successful reproduction.

Consecutive sequence numbers detect interior loss, not a lost final suffix. Do not infer an absent late setter from a clean-looking prefix. A negative claim requires a later verified ATC record that closes the relevant ATC interval; otherwise, keep only positive closed-pair findings and classify missing late-setter evidence as inconclusive. A declared journal end alone does not supply that component-level closure. Do not add a device action or synthetic callback to obtain it.

For a late actual setter, require strict non-overlap: `core_init.end → ATC HOST setter.begin → ATC HOST setter.end(ret=0)`. If intervals overlap, do not infer the order. `dwc3_host_init()` can create a child whose probe completes later; its return alone is not an HCD-completion marker. Use target-scoped pairs and the complete sequence, not timestamp proximity alone to label a caller.

| Observation | Conclusion / next boundary |
|---|---|
| First early call has no handle and returns 0; a later observed HOST setter follows core init | Positive software sequence confirmed. Hardware latching and causal effect remain unproved. |
| Same sequence, but hub/controls appear | Startup behavior varies or instrumentation/packaging changed timing. Not a fix. |
| Valid handle appears only after an earlier failed attempt | Attribute it to the retry; do not relabel it the original first attempt. |
| Unexpected order in positive closed pairs, or no late setter within a separately closed ATC interval | Review actual return/deferred-probe paths; do not add a fix automatically. Without ATC tail closure, a missing late setter is inconclusive. |
| Missing/invalid trace or wrong module identity | Inconclusive; preserve evidence and redesign before another boot. |
| New safety-stop event | Test failure; stop and recover. Do not continue for more data. |

Even complete traces plus both screens and USB are only one diagnostic result. Full Gate 4b, firmware review, Gate 5 reliability, rollback proof, and permanent integration remain separate. No `appledrm` workaround is justified by the existing FIFO observation.

## Correction design — usbdiag2 (LIVING)

The reviewed C0 design remains the contract below. The dated [design record](../evidence/dev-147-usbdiag-correction-design-2026-08-28.md) retains its read-only checks; the [C1 record](../evidence/dev-147-usbdiag-c1-correction-2026-08-28.md) records the later approved source correction and tests. Preserve the failed D3 image, v1 source/fixtures, sealed evidence, and successful working image unchanged. Correcting the missing instrumentation does not explain D3's video failure or add a hardware result.

### Exact target guard and timing boundary

Retain the `apple,j413` board check and its existing whole-string, case-insensitive OF compatibility semantics. In each existing `*_new_generation()` entry, reject a null device OF node, then look up only that component's fixed absolute path with `of_find_node_by_path()`: `/soc/usb@502280000` or `/soc/phy@503000000`. Accept only a non-null lookup result that is pointer-equal to `dev->of_node`; equality alone must not accept two null pointers. Release the lookup reference with `of_node_put()` on every lookup path, before returning, incrementing the generation counter, or logging. Increment only for a match. Do not cache a node pointer, walk raw ancestors, accept a leaf/suffix/near-match, follow a fallback, or add a hardware operation.

The pinned configuration has `CONFIG_OF_DYNAMIC=y` and `CONFIG_OF_OVERLAY=y`; OF kernel unit tests are disabled and must not be enabled for this work. Raw parent walking would bypass the tree's lifetime/locking API. The [exact path implementation](https://github.com/AsahiLinux/linux/blob/e2e1930a9595bffafad92cec2b5504525efb9cd4/drivers/of/base.c#L921) matches whole components, and its [public lookup](https://github.com/AsahiLinux/linux/blob/e2e1930a9595bffafad92cec2b5504525efb9cd4/drivers/of/base.c#L975) returns a reference under the existing OF tree lock. [Dynamic reference release](https://github.com/AsahiLinux/linux/blob/e2e1930a9595bffafad92cec2b5504525efb9cd4/drivers/of/dynamic.c#L34) is real in this configuration. Use that metadata API once per eligible probe, not per log record, at the current generation entry before allocation or existing driver locks. This narrow addition supersedes only the old blanket "no new locks" wording. It permits no driver lock, retry, delay, reordered hardware call, altered return, or zero-timing-impact claim. All compatible devices still bind to these modules; front-port log filtering does not contain their runtime effects to one port.

Use the distinct fixed token `dev147-usbdiag2-v1` in both producers, the strict validator, format/cap expectations, positive fixtures, source/patch pins, and any later build/image manifest. Keep v1 and mixed-revision negative fixtures. Never add an accept-any-revision option or infer the revision from a later loaded binary. The source review predicts replacing the guard-only `strcmp` import with `of_find_node_opts_by_path` and `of_node_put`; both proposed imports exist in the pinned export table. C2 must establish the actual imports and new binary identities, then update exact verifier pins, not permit a broad superset. The old pin set and failed archive stay immutable. Preserve the fixed schema, labels, payload bounds, caps, and private-data exclusion.

### Regression design — production gate through first marker

The existing [logging verifier](../../dev/apple-dp-altmode/usbdiag/kernel/verify_logging.py) extracts constants, reservation, and formatting macros but omits `*_new_generation()`. Its concurrent workload calls both LOG macros with literal generation `1U`. The historical formatting/cap PASS therefore did not exercise the rejecting target guard. Do not repair this with another hand-written predicate or an absolute-path-returning stub.

For C1, extract the actual archived and proposed production generation functions, their reservation path, and the existing probe-entry call through first `probe.begin`, before fallible setup. Pin extraction boundaries and fail closed on source drift. Execute the pinned path-resolution, naming, and compatibility logic, with real pinned [libfdt leaf/unit-name behavior](https://github.com/AsahiLinux/linux/blob/e2e1930a9595bffafad92cec2b5504525efb9cd4/scripts/dtc/libfdt/fdt_ro.c#L303) and small FDT fixtures. Bound the kernel/userspace adapters to allocation, lock/reference bookkeeping, atomics, and output. This is the explicit metadata-test-seam exception: preserve production logic and real semantics, not a hand-written absolute-path stub. It does not simulate PHY, display, USB, boot order, kernel scheduling, or kobject concurrency.

Required RED must fail the correct-target assertion for both frozen v1 guards specifically because generation is zero. A compile, setup, pin, or v2 parser-revision failure is not that RED. Preserve it, then obtain GREEN for the same target matrix with the correction. Keep revision/manifest rejection tests separate from this semantic regression.

Negatives cover missing root/node/target, including null-equals-null; actual rear paths `/soc/usb@382280000` and `/soc/phy@383000000`; `/other`, `/bridge/soc`, and `soc@0`; same-leaf decoys with different pointer/root/path; and unit-address, prefix, suffix, or path-case changes. Board values `apple,j313`, `apple,t8112` alone, and `apple,j413x` reject. `APPLE,J413` must retain the existing API's case-insensitive acceptance, not become a new rejection. Malformed non-null nodes with null string fields are not valid-FDT fixture requirements. Verify balanced references for successful and mismatching lookups, no generation or sequence consumed by rejection, separate retry generations, and reservation/first-marker behavior. Preserve cap/concurrency, malformed-record, identity/revision mismatch, missing-start/tail, and strict drift-negative coverage. A source/fixture mismatch or missing valid RED stops C1; do not weaken checks to obtain GREEN.

Use a fresh private continuation with the previously reviewed containment model, authenticated pinned inputs, bounded processes/output, read-only source inputs, and new output/temp directories. Do not reuse sealed runs, execute public archival helpers, install packages, or run the unrestricted aggregate suite. C1 may create userspace test executables and small synthetic metadata fixtures only; it excludes kernel module binaries, boot images, staging, and live probing. Preserve all failures and run the scoped SWE → QA → Review loop before proposing C2.

### C1 result — source-only correction (LIVING)

C1 is complete within its approved source/userspace-test scope. The two archived v1 correct-target tests first failed specifically at generation zero, with sequence zero and no marker; setup and compilation succeeded. The current v2 guards now implement the exact target/reference contract above. A pre-test review added the explicit null-device early return; its two valid-board null-node regressions require only the board lookup's single balanced reference, with no target lookup.

Focused QA and independent source/test review PASS: 10 target methods retain 54 synthetic-FDT executions; 65 strict trace tests pass; format/cap checks cover 57 call sites and ten eight-thread rounds. The [dated C1 evidence](../evidence/dev-147-usbdiag-c1-correction-2026-08-28.md) owns commands, pins, results, and trust limits. The harness executes exact pinned OF and producer fragments with real libfdt naming, but its metadata/atomic/output adapters do not prove kernel concurrency, timing, or hardware behavior.

Current producers, target/logging tests, validator, and new v2 patch/schema use `dev147-usbdiag2-v1`. Historical `usbdiag1.patch`, `usbdiag1.schema.json`, `verify_modules.py`, and `build/build-diagnostics.sh` remain unchanged. The binary verifier and build helper retain v1 pins and are not suitable for v2. The separate C2 result below establishes actual new module imports and binary identities without broadening those old pins. No `.ko`, image, staging, reboot, hotplug test, or live probe occurred in C1.

### C2 execution boundary — approved offline work (LIVING)

David's 2026-08-28 continuation authorizes C2 preparation, focused QA, and independent review until manual support is needed. This is approval, not a C2 PASS. Revalidate pinned source, headers/configuration, toolchain, working-image inputs, and containment before a fresh private build. Keep all old sources, controls, failed images, evidence, and working boot files unchanged.

Prepare fresh unmodified and v2 diagnostic module pairs, and only the E image with packaged DWC3 and unchanged ATC; B/G images remain unprepared pending concrete later review. Establish actual ABI/imports, hashes/build IDs, unchanged-operation review, exact no-change/delta archive checks, and complete dependency/alias resolution. Do not reuse v1 build or binary-verifier pins for v2. Preserve every failure and keep the scoped SWE → QA → Review loop. No package install, live module load, persistent boot change, or unrestricted fallback follows from this approval.

After the offline exits pass, prepare the exact staging and recovery handoff for review. Stop for David when monitor connection, sudo, or reboot is required; do not execute those actions or replay a consumed handoff. C3 staging and C4 image selection remain separate manual steps with fresh readiness checks. W/E/B/G are not a boot schedule. Monitor disconnection during offline work is permitted, not confirmed or counted as a test. External submissions remain unauthorized.

### C2 result — offline artifacts (LIVING)

The [dated C2 record](../evidence/dev-147-c2-offline-preparation-2026-08-28.md) owns the fresh control/v2 module identities, exact five-symbol import additions, retained REDs, focused GREEN tests, and independent QA. Both module pairs are private and untested in the kernel. The new strict v2 verifier passes 15 methods and 55 retained child-command contracts. Old v1 build and binary-verifier sources remain unchanged; their identities were not broadened.

Only E was assembled. It adds the exact packaged DWC3 module to W, replaces only the dependency/alias binary indexes, and preserves raw ATC, the working patched TIPD core, all 199 old modules, and unrelated records. The E helper passes 13 methods; its actual assembly passes all 414 real child commands and independent image/index QA. No diagnostic or rebuilt control module is in E. B/G images remain unprepared pending concrete later review. This is not a boot-order measurement, display/USB fix, or hardware safety result.

The C2 source checkpoint `b1414e3f3fecd4dadcaea32070cac29cde8e2b0b` is pushed and verified; its 2,270-file private seal passes. Keep those outputs immutable. C2 did not stage or boot E; the later C3 staging result below owns that separate action.

<a id="c3-result--offline-staging-preparation-living"></a>

### C3 result — offline staging preparation (historical)

Historical preparation checkpoint. Since 2026-08-28, the [user-run staging result](#c3-staging-result--user-run-pass-living) below supersedes the pending action in the following preparation text; do not replay it.

The [dated C3 record](../evidence/dev-147-usbearly-staging-helper-2026-08-28.md) owns the new E-only helper and four exact source exports. Two genuine assertions first rejected the old image/proof constants with valid setup and successful collectors. After the minimal correction, syntax and all 42 methods pass, including the 38 unchanged historical bodies and four new fixed-identity/producer/completion contracts. Independent saved-run QA, helper review, and exact three-assignment private-copy review pass. There was no test replay, production preflight, or device action.

The helper preserves all 32 old protected rows and adds the retained failed-v1 image. Its eight proof rows bind the sealed C2 checkpoint and actual E result. The tested 33 + 8 unique rows are producer assertions, not fresh protected-file reads. The public copy stays deliberately nonoperational; the private copy's remaining bytes equal the tested source. Both fixed image/proof identities and the private-copy comparison were independently checked. E contains no diagnostic or rebuilt control module and remains unstaged/unbooted.

Next is David's one exact private staging-only command under the [main handoff](dev-147-m2-displayport.md#current-handoff--c3-staging-only-living). No monitor connection is needed. Keep current cables unchanged, MagSafe connected, battery strictly above 50%, the lid open, and the internal screen usable. Stop on refusal or missing completion; retain outputs and backups, with no cleanup or retry. Review the full protected/image post-check result and exit 0 before any separate C4 proposal. No image selection or reboot follows automatically.

### C3 staging result — user-run PASS (LIVING)

David completed the reviewed private staging command. The [dated staging evidence](../evidence/dev-147-usbearly-staging-2026-08-28.md) records all 41 exact, ordered initial-preflight rows and the complete final report with a normal prompt. No separate numeric exit status was captured. Qualified receipt QA accepts successful user-run validation: later repeated checks, logs, and markers are root-private and were not independently read. Independent checks match 37 readable rows and the unchanged source/helper identities; E's root:root mode-0600, single-link, 19,191,513-byte metadata agrees. The destination bytes and four older protected files remain unreadable to the agent. The full C3 seal and fixed pins pass.

E was staged and unbooted at that checkpoint; David has since confirmed selecting it for C4. No diagnostic or rebuilt control module is in it. The staging handoff is consumed. Do not retry it, alter any artifact, or request another sudo read merely to duplicate successful validation. Preserve the offline preparation record, old images, both backups, and all evidence. Normal boot selection stays unchanged; this is not full DTB rollback or a runtime test of the macOS restore bundle.

The unknown-setup hold at that staging checkpoint is superseded by the C4 readiness result below. This does not change C3's provenance or permit staging replay. D3 causality remains unknown, startup USB/full Gate 4b stays HOLD, and B/G images remain unprepared.

<a id="c4-readiness--conditional-e-handoff-living"></a>

### C4 readiness — conditional E handoff (historical)

Readiness checkpoint only. The following pending instructions are superseded by the post-reboot selection HOLD below; do not replay the intended E case.

David confirms MagSafe, the front/lower left monitor connection, both physical images, normal responsiveness, and empty monitor USB ports. The [dated readiness record](../evidence/dev-147-usbearly-boot-readiness-2026-08-28.md) owns matching power, kernel/package, integrity/seal, metadata, display, and loaded-identity checks. Independent readiness QA and exact handoff safety review pass. No new USB-enumeration or full-log claim is made. E is unbooted; its loaded module IDs cannot distinguish it from W. Any later result requires a new boot identity and David's exact selected filename.

The [current main handoff](dev-147-m2-displayport.md#current-c4-handoff--one-user-selected-e-boot-living) is the sole exact procedure. David confirms saved work and the Mac guide on another device, and requests reboot when ready. Final task release, the complete new E/W handoff available/read outside Linux, and his choice of this exact case remain required; no extra chat reply is intrinsically required. Keep the verified setup unchanged and stop for renewed review on drift. Zero diagnostic markers are expected for E. Missing USB alone with healthy displays is HOLD, not a W recovery trigger. Safety loss takes priority over capture; the separate one-use W recovery requires responsive Linux, a usable screen, and a safe restart. The offline Mac fallback and all stop/cancel/no-retry rules remain explicit in the main handoff.

<a id="post-c4-reboot--display-loss-selection-unconfirmed-living"></a>

### Post-C4 reboot — display loss, selection unconfirmed (historical)

This was the initial attribution hold. The selection addendum below resolves it without changing the saved failure evidence.

The [dated result](../evidence/dev-147-post-c4-display-loss-2026-08-28.md) records David's internal-screen/responsiveness PASS and external-image loss. Private comparison verifies a new boot. The 16-page, 1,012-record kernel envelope is ordered and unique; its scan-end cursor caveat and logging limits remain explicit. Packaged DWC3/ATC and patched TIPD IDs match both W and E, so the exact selected filename remains essential. Root hubs alone enumerate. Zero diagnostic markers are expected for E and do not identify the image or constitute trace failure.

The [main plan's current HOLD](dev-147-m2-displayport.md#current-handoff--post-reboot-selection-hold-living) requires exact filename confirmation. The intended E handoff is consumed. W recovery is prepared and conditionally reviewed only for confirmed E selection; it is not released or performed. Missed, other, or uncertain selection must be resolved first. Keep cables unchanged; no retry, reconnect, extra test, or duplicate privileged read. D3 causality and full Gate 4b/USB remain unresolved.

<a id="e-selection-confirmed--w-recovery-prepared-living"></a>

### E selection confirmed — W recovery prepared (historical)

The following preparation is superseded by the observed recovery below. The one-use W handoff is consumed; do not repeat it.

David confirms the requested E filename. The [dated addendum](../evidence/dev-147-c4-selection-confirmation-2026-08-28.md) combines that confirmation with the saved new-boot capture and fresh same-boot readiness checks. E's external-display criterion failed; no hardware cause or call order is established. No new full-journal or USB capture was made. Zero markers remain expected for uninstrumented E.

The reviewed R-E selection condition is satisfied. The [main W handoff](dev-147-m2-displayport.md#current-recovery-handoff--working-image-w-living) prepares one manual recovery for final release, with saved work, offline instructions, unchanged setup, responsive Linux, a usable screen, and safe restart required. W has not been performed. E is consumed; no retry, reconnect, B/G test, or other device action follows.

### Recovery after E — display restored, attribution qualified (LIVING)

The [new dated record](../evidence/dev-147-w-recovery-after-e-2026-08-28.md) combines David's restored-image report with a fresh boot, both native outputs, root hubs only, matching readable integrity pins, and a bounded 1,122-record kernel envelope. He did not restate the filename; the shared W/E IDs are consistent with W but do not independently prove that artifact started. Known firmware diagnostics and the scan-end cursor caveat remain explicit. Zero diagnostic markers are expected; no call-order, clean-firmware, reliability, or causal result follows.

The [main current state](dev-147-m2-displayport.md#current-state--display-recovery-observed-living) consumes the W handoff. USB/full Gate 4b stays HOLD. The new goal permits reviewed isolated offline driver tests/builds only, not live driver/display tests or helper replay. The separate login-focus observation needs its own approval and disposable tests; no greeter work belongs to this goal.

### Autonomous offline technical gates (LIVING)

The [main goal](dev-147-m2-displayport.md#autonomous-offline-goal--next-test-package-living) owns scope, exclusions, and completion. Apply these gates in order after plan review; none grants live authority.

| Gate | Required work and exit |
|---|---|
| A0 — evidence and inputs | Inventory and pin saved W/E/D3 captures and exact patched TIPD, DWC3, and ATC sources. Compare startup observations and control-flow differences. Record competing hypotheses, the latest W filename qualification, capture limits, and missing measurements; do not infer causality from temporal order. |
| A1 — one selection | Independent review selects one justified minimal candidate, or one diagnostic if the cause remains unproved. Selection is a gate, not an obligation to fabricate a fix. Diagnostics must preserve hardware operations; a behavior-changing candidate needs explicit minimal source justification and new design review. Record the expected observation, blast radius, acceptance and stop rules. No automatic E retry, B/G ladder, or speculative image. |
| A2 — contained implementation | Verify a fresh unprivileged sandbox before work. Use fixed tool/input pins, existing read-only inputs, fresh private outputs, and bounded focused tests. Implement/test/build only the selected artifacts; preserve genuine RED/GREEN for code changes. Check exact ABI/import bindings, module identity, archive/index deltas, stock preservation, and real-tool/no-change controls. Retain all failed attempts. Kernel/dependency drift stops work; no guard relaxation, installation, silent repinning, or unrestricted fallback. |
| A3 — manual-boundary package | Complete offline: accepted image, 54-method staging and 13-method collector/binding results remain unchanged. The [sealed handoff](../evidence/dev-147-t1-manual-package-2026-08-30.md), later [user staging](../evidence/dev-147-t1-user-staging-2026-08-30.md) and [attended T1 result](../evidence/dev-147-t1-boot-capture-2026-08-30.md) are separate records. No replay. |
| A4 — review and seal | Independent handoff, manifest, safety and 89/89 seal QA passed. The seal is unchanged. Later user staging and the separately approved T1 case are now consumed. Any W recovery or new live test needs separate approval. |

If bounded A0/A1 investigation establishes an indispensable missing observation or authority, independently review a NO-GO dossier with the exact minimal next capture/action. Seal and publish those findings and sync DEV-147. This alternative is not a completed test package, nor a shortcut for uncertainty, slow work, or failing tests. Preserve old seals, images, operational helpers, and every dated result. New isolated helper execution does not permit historical replay, production preflight, or live writes. A later mouse/device test needs hub enumeration and separate review; keep downstream ports empty now. SDDM fixes remain separate.

#### Remaining collector exit criteria (LIVING)

The main plan's [minimum remaining path](dev-147-m2-displayport.md#minimum-remaining-path-living) owns priority, reuse and the next approval boundary. The [fixed collector/binding](../evidence/dev-147-t1-capture-2026-08-30.md) retains its 13 focused offline tests and independent reviews. Its one actual [T1 invocation](../evidence/dev-147-t1-boot-capture-2026-08-30.md) now passes the qualified capture checks and is consumed. The criteria below remain the contract, not an instruction to repeat tests or collection.

- Successful capture uses the minimum fixed-T1 bounded child collector and existing accepted formats/validators. Bind the actual boot and exact accepted artifact identities; receipts must describe the child that actually ran, without fixture identity overrides.
- Wrong, missing or mixed boot/artifact identity must refuse acceptance. Test the successful binding and these refusals on focused offline fixtures.
- Child error, timeout and stdout/stderr size limits must stop acceptance. Enforce the fixed bounds, terminate the process group when required and reap the direct child. Cleanup means process cleanup, not deleting retained evidence or retrying.
- Interruption/truncation must retain partial output and remain inconclusive where the trace is incomplete. Test private exclusive/no-overwrite publication, collisions and failure during publication; a pathname alone must never count as an accepted capture.
- Reject false completeness or absence claims. A structurally complete prefix cannot rule out an unseen closed suffix; HPD call return does not prove receiver delivery.

The consolidated package review and seal pass; the [dated package record](../evidence/dev-147-t1-manual-package-2026-08-30.md) owns that result. The later [user-run staging receipt](../evidence/dev-147-t1-user-staging-2026-08-30.md) is accepted and consumed. Follow the main plan's current attended-test approval boundary. Neither result is fresh live authentication or evidence that a collector ran on T1.

<a id="a1-proposal--t1-tipd-sender-diagnostic-living"></a>

### A1 selection — T1 TIPD sender diagnostic (LIVING)

Selected for offline A2–A4 after independent safety PASS, exact four-document QA PASS, and source/design review agreement. The [dated A0 investigation](../research/dev-147-hpd-startup-2026-08-29.md) owns source pins, saved timing, and competing explanations. No behavior fix is selected. T1 asks whether the front-controller worker chooses and returns from the connected-HPD call, with the cached state and actual mux/role outcomes that precede it. It cannot prove that DRM delivered the notification or that a PHY latched a setting. The selection checkpoint initially had no T1 source, binary, or image. The later [private candidate checkpoint](../evidence/dev-147-t1-private-candidate-2026-08-29.md) records accepted source and a reproducible private module. The candidate was never bound into E control. The [third operational attempt](../evidence/dev-147-e-operational-third-attempt-pass-2026-08-30.md) supplies the required fresh E no-change proof after two retained fail-closed attempts. The later authorized [private T1 assembly](../evidence/dev-147-t1-private-image-2026-08-30.md) and independent read-only result QA passed. One private T1 image exists. The [full offline staging result](../evidence/dev-147-t1-staging-full-offline-2026-08-30.md) passes 54 methods, and the [fixed collector/binding](../evidence/dev-147-t1-capture-2026-08-30.md) passes 13 focused tests. Both have independent result/source/boundary review. The [manual package](../evidence/dev-147-t1-manual-package-2026-08-30.md) is now reviewed and integrity-sealed for the first manual staging/review boundary only. That offline review released no staging or manual action. Later user staging and the [attended T1 result](../evidence/dev-147-t1-boot-capture-2026-08-30.md) are separate, consumed events.

#### Single candidate and preservation boundary

T1 is one distinct TIPD sender-only diagnostic, revision `dev147-tipddiag1-v1`, with E-equivalent early availability of the exact packaged DWC3 module. Its private image derives from authenticated retained E, SHA-256 `4d4f0557af57eebcc33322f004bcc7968254e644b069a11102375df0b31a52ae`. Under the [fixed assembly contract](../../dev/apple-dp-altmode/usbdiag/tipd-image/t1-assembly-contract.md), only the TIPD core payload may differ; all seven index payloads remain byte-identical. This tightens the earlier selection's possible justified-index delta. Packaged ATC, DWC3, I2C frontend, graphics modules, DT, kernel, hooks, and configuration remain unchanged. There is no module addition, W diagnostic image, E/D3 replay, or B/G ladder. The image identity is retained in the dated assembly evidence. Fixed staging constants and private-file transaction behavior are covered by the full 54-method run, with independent result QA and executed-boundary safety review PASS. The handoff remains held; these are not live instructions.

Only `drivers/usb/typec/tipd/core.c` may gain the T1 instrumentation. Use the working HPD-patched source as its base; preserve that patch's hardware behavior. This is the sole approved exception to the old DWC3/ATC-only restriction. Keep common headers, `trace.c`/`trace.h`, frontend source/binary, DWC3/ATC diagnostics and their validators, DCP/DRM, and all historical artifacts unchanged. No early PHY acquisition, new error handling, reordered calls, register reads/writes, polling, sleeps, delays, retries, HPD replay, new work, or new driver lock is permitted.

#### State, target, and allocation contract

The pinned I2C frontend allocates `data->tps_struct_size` before calling `tipd_init`. Use a core-local wrapper with unchanged `struct cd321x` at offset zero and a diagnostic tail. Change only `tipd_cd321x_data.tps_struct_size` to allocate that wrapper. Do not change the layouts of `tps6598x`, `cd321x`, `tipd_data`, or `sn201202x`, any other data table, or exports. Both CD321x instances have the larger allocation, even though only the selected front instance logs. Lifetime stays with the existing devm allocation; no global stale-pointer map, extra callback, or new work item.

Check exact `tps->data == &tipd_cd321x_data` before any wrapper conversion or tail access, including shared paths and error exits. At `tipd_init` entry, use the existing `apple,j413` board semantics and one exact referenced-node check for `/soc/i2c@235010000/usb-pd@3f`: reject null, require a non-null lookup and pointer identity, and release the OF reference before counter allocation/logging. Do not compare leaf text with an absolute path, follow ancestors, cache the OF pointer, or add another lookup in the event path. Preserve the board API's case semantics. The metadata guard uses existing OF locking/refcounts and can affect timing; this is not a zero-impact claim.

Initialize diagnostic state before any work can queue. Each selected `tipd_init` invocation gets a generation; each worker gets a distinct worker ID. Use existing locks and independently reviewed atomics for diagnostic counters, without changing driver lock order. The earliest marker means **TIPD core initialization entry**, not frontend probe entry: allocation, regmap, or other frontend failures before `tipd_init` remain outside coverage.

#### Required records and interpretation

Use native kernel INFO/JSON, fixed revision `dev147-tipddiag1-v1`, board `j413`, target `front_lower`, and component `tipd`. A global sequence budget permits at most 128 T1 records, including one reserved terminal cap record; the complete emitted message must be at most 384 bytes. Do not reset the budget across init retries. Rejected targets consume no generation or sequence. Every hardware operation must execute regardless of target selection, log budget, or cap state; never put an operation inside a conditional log argument.

| Existing boundary | Required observation |
|---|---|
| `tipd_init` | Positive entry marker and explicit original success/failure outcome; generation includes failed initialization and later retry. |
| Existing status/queue path | Cached/stored and queued connection, USB2/USB3, HPD/change, orientation, and role facts already available as bounded booleans/enums; no extra hardware read, partner identity, raw register dump, or pointer. |
| Worker entry / terminal | Snapshot with worker ID; every terminal reason, including disconnected, partner-registration failure, and normal completion. |
| Mux selection | Actual `typec_mux_set` calls and their existing return values, separately from unchanged-mode or other no-call paths. Preserve the void caller and ignored-error behavior. |
| Role changes | Begin/return for both the conditional `USB_ROLE_NONE` call and final role call, including no-call decisions. Errors remain ignored exactly as before. |
| HPD | Both disconnected and connected call/skip decisions, with their cached conditions; begin/returned around each actual call. The API returns void: never label return as delivered or accepted. |

Do not add `drm_connector_find_by_fwnode`: it is not exported here, and a second lookup would race the dispatcher's real lookup. The existing Apple receiver marker supports only positively correlated callback entry. It lacks T1 revision/target identity; absence remains inconclusive. Neither “DCP booted” nor `is_main_display` is a registration-ready marker. Source proves pre-registration drop only **if an actual send occurred before registration**. Later eligible TIPD work can resend connected HPD; do not infer permanent loss or invent an edge-only contract.

#### A2–A4 acceptance before any manual boundary

The accepted results satisfy this retained offline contract, including the [collector exit criteria](#remaining-collector-exit-criteria-living) and the later [manual-package review and seal](../evidence/dev-147-t1-manual-package-2026-08-30.md). Apply the main plan's [reuse and revalidation rule](dev-147-m2-displayport.md#minimum-remaining-path-living). No live acceptance is implied.

1. Freeze exact source/tool inputs and review a fresh C2-equivalent 582-entry containment manifest and launcher relocation. Require a new successful isolation/smoke probe before workloads. Use narrow read-only inputs, fresh private outputs, bounded commands, and at most two compiler jobs. No install, silent repin, widened mounts, old-helper replay, production preflight, or unrestricted fallback. A3's separately approved v6 preserves all 582 v5 rows and adds only sync; it is not authority for any other runtime change.
2. Retain genuine semantic RED before correction/implementation, then GREEN in focused userspace tests. Execute extracted production target/variant/wrapper/init/worker logic with pinned OF semantics and bounded adapters. Cover first entry, null/wrong board/path/variant, wrapper access safety, unchanged common offsets, init retries, independent instance/worker IDs, every early return, actual mux versus mode skip, ignored role/mux errors, and cap/concurrent formatting. Operation ledgers must match original call count/order/results; cap and rejection must not suppress operations. Setup/pin/compile failure is not meaningful RED, and these tests do not simulate hardware safety.
3. Add a separate strict T1 parser/schema and fixtures. Bind exact image/module/revision/target/component identities; reject old or mixed revisions, malformed/duplicate/oversize records, wrong generations/workers, gaps, missing positive entry, missing operation pairs, missing required worker/init terminals, or a missing cap record when the budget is exhausted. Preserve valid source-permitted interleaving of init and worker records. An incomplete capture stays inconclusive with no negative sender claim. A complete sender trace still cannot assert receiver delivery or a USB/video fix. Preserve existing v1/v2 validators and fixtures.
4. Build fresh uninstrumented working-HPD TIPD control and T1 core with unchanged headers, trace sources, and frontend. Independently verify actual ELF/BTF, identities, exported layouts/offsets, wrapper offset/size, and exact binding-aware imports/exports against source-derived expectations. Zero `CONFIG_MODVERSIONS` CRCs do not prove ABI compatibility. Compare the frontend's allocation contract and unchanged non-CD321x data tables; neither metadata equality nor unmodified source proves packaged-byte identity.
5. Prepare only the distinct T1 image after those gates. Require no-change E reconstruction, exact raw-record/metadata preservation outside the single TIPD payload delta, byte-identical retained indexes, and real dependency/alias/symbol-index checks. Controls are offline evidence, not extra boot cases. Independently review the precise image/hash/size and preserve E, W, D3, stock files, and their seals unchanged.
6. Reuse the accepted staging-fixture and collector results and unchanged [manual-package seal](../evidence/dev-147-t1-manual-package-2026-08-30.md). The separate [user-run staging PASS](../evidence/dev-147-t1-user-staging-2026-08-30.md) is consumed. Stop for fresh readiness/input authentication and a separately approved attended T1 proposal under the main plan. The live collector remains held and unrun. No staging replay, reboot, cable/device, recovery rehearsal, mode/suspend or greeter action is authorized now.

Execution-boundary checkpoint, 2026-08-29: accepted RED `run-0zk61la1` and corrected GREEN `run-nr5woop4` prove the exact authenticated bootstrap, eight-input/593-mount production model, and bounded collectors without running a workload child. First GREEN `run-_l2w9p_k` remains retained with its Python 3.14 `Path`/`PosixPath` stop. The accepted subject SHA is `39496435f113c7d9256e5592effd3fece8c52b0e61b774e8283fe96eb84d4add`. The three real operational APIs remain unavailable. A separate review must approve the exact 424-child E no-change control before it runs.

Historical assembly checkpoint, 2026-08-30: the [sole private T1 assembly](../evidence/dev-147-t1-private-image-2026-08-30.md) and independent read-only result QA passed. Pre-execution review and exact-nine-input probe `run-vpqm7rk3` preceded `run-mvqmtbw_`. Six children completed: gzip `-n`, direct build-ID description and retained-index T1 lookup. The image has exactly one TIPD payload delta, seven unchanged indexes, and 7 early/1,163 main records. Complete validated JSON, outer exit 0, no timeout, unchanged inputs and independent candidate/readback audit pass; final-file presence alone is never PASS. The [fixed T1 boundary](../../dev/apple-dp-altmode/usbdiag/tipd-image/t1-assembly-contract.md) binds real candidate/build/proof bytes. It reuses accepted [E proof](../evidence/dev-147-e-operational-third-attempt-pass-2026-08-30.md), not a raw-child audit or E rerun. Both failed E attempts, [zero-child RED/GREEN](../evidence/dev-147-t1-assembly-preexecution-2026-08-30.md), and independent fixture QA `run-zbpyg548` remain history. At that checkpoint A3 preparation was next. The later [full staging fixture record](../evidence/dev-147-t1-staging-full-offline-2026-08-30.md) now owns the offline staging result; capture and manual-package holds remain. No index generation, assembly retry, staging, load, boot, hardware or manual action is released.

### Comparison design — separate availability, rebuild, and instrumentation

These are comparison definitions, not a boot schedule. C2 prepared only E; B/G images remain proposals. Each uses the same frozen working DP core, DT route, kernel, hooks, configuration, and unrelated archive records. Preserve raw archive bytes where unchanged. Strictly justify any index delta with real dependency/alias resolution; no preload, custom hook, kernel-argument change, or normalization is part of these controls.

| Label | Proposed contents | Comparison limit |
|---|---|---|
| W | Frozen working image: packaged ATC, no DWC3 glue in the image | Existing recovery baseline, not a fresh timing-matched run |
| E | W plus the exact packaged DWC3 module and only strictly required indexes; ATC bytes unchanged | W/E separates the early-availability packaging bundle, not boot order by itself |
| B | E topology with both DWC3 and ATC rebuilt unmodified from the pinned source under the same reviewed toolchain used for G | E/B exposes the rebuild/toolchain bundle; unmodified source does not guarantee packaged-byte identity |
| G | B topology with the complete corrected two-driver diagnostics and strict `dev147-usbdiag2-v1` identity | B/G compares the instrumentation bundle, including the guard metadata lookup and logging timing |

The no-change archive control is separate: exact reconstruction proves bytes and indexes, not probe order or hardware safety. The failed D3 image is not a logging-disabled control; it combines different binaries and earlier availability despite its suppressed output. A source-order defect, successful recovery, or single image result does not establish hardware causality. E was the first conditional single-case proposal because it isolates the smaller early-availability bundle without new diagnostic code. E and the later intended W recovery handoffs are consumed. No E → B → G boot ladder or further recovery attempt is scheduled.

The working [TIPD HPD patch](../../dev/apple-dp-altmode/tipd-cd321x-hpd.patch) retains a mux update, then a USB-role call, then the conditional connected-DP notification. Earlier DWC3 availability can change role-callback availability/timing before that notification. This is a source-supported timing risk, not evidence of which path ran or failed in D3. Matching module metadata does not make rebuilt binaries byte-identical to packaged binaries. Do not try to recreate late loading with forced preloads, delays, root-module replacement, or boot hooks; each would add another uncontrolled change.

### Named review gates

| Gate | Permitted scope only after its own approval | Exit before the next proposal |
|---|---|---|
| C0 — design complete | Read-only source review and this documentation reconciliation | Independent guard/test-semantics and comparison/safety design reviews agree; no executed-test or hardware PASS |
| C1 — source-only complete | Approved contained source correction and focused userspace tests; no `.ko` or image | Genuine production-path RED/GREEN, reference/revision/drift coverage, focused QA, and independent source/test review PASS |
| C2 — offline checks and seal PASS | Fresh unmodified/v2 module pairs and E only; independent QA and review complete | Exact identities/imports, unchanged operations, archive controls, and dependency resolution pass; sealed artifacts remain unchanged |
| C3 — user-run staging PASS | Reviewed E-only command completed by David; qualified receipt QA and independent metadata/readable checks agree | Final report evidences repeated protected/image checks; no independent root-log or numeric-exit capture. E is staged; handoff consumed |
| C4 — E external-display FAIL | Selection confirmed; saved boot capture and readiness checks agree | Later intended W recovery restored both displays with filename attribution qualified. Both handoffs consumed; no retry or extra test. Full USB acceptance stays HOLD |

E and B are uninstrumented controls. Assess their exact image/module identities, both physical screens, target USB enumeration and power, and bounded ordinary kernel/firmware logs. Zero `dev147` markers are expected; their absence is not a missing-trace failure and supports no call-order claim. Only G must meet the strict v2 first-probe/start, sequence, operation-pair, and tail criteria. Missing USB enumeration remains a reported HOLD for every image, not an accepted fix.

Before any future image selection, its exact separate recovery handoff must already be reviewed, approved, and available offline. Do not defer recovery design until failure. Approval never advances the next gate automatically. C4 cannot reuse an old GRUB handoff, assume normal boot restores the DTB, or add a reconnect, mode test, USB-device test, suspend, live swap, automatic retry, or extra test boot. All historical stop conditions remain. Review full firmware and journal envelopes for each image; apply diagnostic-trace completeness and absent-start/tail uncertainty only to G, separately from hardware behavior. Full Gate 4b, reliability, rollback proof, release, and upstream contribution remain separate.

## Offline QA before any boot approval

Historical v1 tests remain PASS within their recorded scope; they did not exercise the OF leaf-name semantics in the target predicate. C1 supplies the separate source-level regression result above. David's new C2 approval permits offline preparation under the requirements below and fresh containment; C1 GREEN alone did not advance the gate. Staging and boot remain manual boundaries, not consequences of a successful build.

Review the source diff against the exact pinned base. Check that every original hardware operation, call count/order, lock boundary, error/no-op path, and return behavior remains intact. Logging must not make a formerly ignored failure actionable. Verify all print formats and record-length bounds; no raw data is permitted.

Use parser fixtures for null and error handles, first-attempt failure followed by retry, independent probe generations, late and overlapping setters, asynchronous child probing, missing starts/tails, sequence gaps/duplicates, caps, malformed JSON, wrong controller/revision/build ID, and explicit failed operations. Include fixtures that remove the final complete ATC setter pair or final cap marker while leaving prior sequences consecutive; neither may support a negative late-setter claim. Fixture results do not simulate or prove hardware behavior. Check the cap under concurrent emission without changing driver synchronization.

The new image helper needs real archive-tool round-trip/delta checks, including the no-change reduced-index control and early/main archive boundaries, plus real dependency resolution. Add failure fixtures for unsafe paths, symlinks/hardlinks, existing outputs, drift, missing index inputs/dependencies, unexpected files/configuration, and incomplete builds. Keep real-tool failures distinct from parser fixtures. No hardware mocking may be used as evidence of display or USB safety.

Run focused tests only inside the reviewed sandbox. The earlier aggregate suite has five recorded failures and includes a fixture that attempted a credentials-path write. Do not rerun it unrestricted or call this a release/full-suite pass. New failures block release unless explicitly waived; existing failures remain visible. Use the repo's SWE → QA → Review loop, with at most three correction rounds.

## Rollback and retained state

Before D3, leaving the separate image unselected preserved normal startup. That preparation state is now historical: David selected it and reported failure. Retain both images and all evidence. Do not delete or overwrite anything to undo the experiment.

The separately reviewed [recovery handoff](dev-147-m2-displayport.md#current-recovery-handoff--previous-working-dp-image-living) is consumed. David selected `initramfs-linux-asahi-dpalt.img`, without `usbdiag1`, and the [validated result](../evidence/dev-147-dp-recovery-2026-08-28.md) is one functional display PASS. This restores the working patched DP core with packaged USB drivers, not the stock Type-C core or original DTB. A normal unedited restart instead selects the stock driver image and may lack external video. The [full rollback gate](dev-147-m2-displayport.md#gate-6--prove-full-rollback-then-retain-the-evidence) remains a separate reviewed action. Keep both timestamped backups, both images, and the offline macOS bundle; Mac restore execution remains untested. The newer intended W recovery after E is also consumed, with observed display recovery but qualified filename attribution. No recovery replay, cleanup, package install, persistent boot change, or live checkout edit is authorized.

## Decision Log (LIVING)

- 2026-08-30: Record the separate [crash-flag preparation](../evidence/dev-147-crashflag-preparation-2026-08-30.md) without changing the accepted USB/T1 source, images or consumed capture. The main plan owns its held manual boundary. No USB, live-probe or hardware acceptance result follows from focused offline tests.

- 2026-08-30: Supersede the temporary LG 27 working state with the [spontaneous-dropout record](../evidence/dev-147-lg27-spontaneous-dropout-2026-08-30.md). David confirms spontaneous loss, then unsuccessful reconnect, with internal screen/system normal and the other cable functional elsewhere. Saved-file QA passes: connected/disabled DP-1, 16 modes, positive atomic EINVAL evidence and 16-slot AFK exhaustion. Pinned source and matched-module disassembly make the poweroff latch the leading recovery hypothesis, not proof. Prioritize one bounded rejection-branch diagnostic design; no tracing, mode request, reboot, patch or upstream submission is released. Preserve all previous records.

- 2026-08-30: Record [LG 27 4K60 video after David's cable change](../research/dev-147-lg27-link-investigation-2026-08-30.md), verified in the unchanged T1 boot with independent saved-file QA. Preserve initial failure, logging cap and explicitly partial broad post-swap output; use the valid narrow final window. Keep working hardware unchanged. Stale EDID and physical/cable identification remain separate gaps; no startup/reliability or generic driver-fix claim follows.

- 2026-08-30: Record the authorized [LG 27 read-only diagnosis](../research/dev-147-lg27-link-investigation-2026-08-30.md), independent saved-file QA and pinned-source investigation. Same-boot old-monitor success removes the different-image explanation for these outcomes, not the cable confounder. Preserve original records, source and images. Follow the main plan's exact-model/readiness and one-cable comparison boundary; no behavior patch, instrumentation build or further live action is released.

- 2026-08-30: Apply the [monitor/cable correction](../evidence/dev-147-t1-monitor-identity-correction-2026-08-30.md) to living status only. Preserve T1 raw logs, all earlier evidence and qualified identity/capture PASS. T1/new-monitor versus W/original-monitor is not a matched driver comparison. The main plan owns the proposed current-monitor snapshot and separately approved cable cross-test. No further live or manual action follows from this correction.

- 2026-08-30: Accept [T1 user-run staging](../evidence/dev-147-t1-user-staging-2026-08-30.md) after exact saved stdout reconstruction, empty stderr, numeric exit 0, and independent receipt QA PASS. Root-private destination/preservation checks remain the user's pinned privileged-helper checks. Consume staging without replay; T1 is UNBOOTED. The next attended connection/selection/capture proposal and conditional W recovery need separate approval under the main plan.

- 2026-08-30: Accept the [T1 manual-boundary package](../evidence/dev-147-t1-manual-package-2026-08-30.md) after independent handoff, manifest, safety, and final 89/89 checksum review PASS. Reuse accepted artifacts and 54/13-method results without replay. A3/A4 now supplies a sealed held package, not live authority. Follow the main plan's first manual staging/review boundary; any later T1 boot/capture or conditional recovery needs its separate release.

- 2026-08-30: Complete the approved minimum fixed-T1 collector/binding offline. Preserve the three-test RED and sole [13-test GREEN](../evidence/dev-147-t1-capture-2026-08-30.md), with independent result/source/boundary reviews. Reuse accepted artifacts and suites. Next is the consolidated held handoff/recovery review and seal, not a live run. A3/A4 remains incomplete; every live/manual hold remains.

- 2026-08-30: Follow the main plan's [minimum remaining path](dev-147-m2-displayport.md#minimum-remaining-path-living). Add focused collector/binding exit criteria without reopening accepted builds, image assembly or staging fixtures. Preserve fresh authentication, new-workload review/probe, independent QA/safety review and every manual hold. This is a plan-only change.

- 2026-08-30: David approved the exact sync-only runtime addition and full offline staging tests after disclosure that sync -f flushes the containing filesystem. Preserve v5 and all source/test bytes; v6 adds one verified executable to its 582 unchanged rows. Fresh probe `run-mvrkgtqy` preceded the sole [54-method run `run-nwf2rtvo`](../evidence/dev-147-t1-staging-full-offline-2026-08-30.md), which passed with 200 expected direct fixture Bash child results and unchanged inputs. Independent result QA and executed-boundary safety review passed. This supersedes only the prior runtime/full-suite hold, not its history or any live gate. Next is the missing fixed-profile capture/binding implementation and then A4 review/seal; the package and overall goal remain incomplete.

- 2026-08-30: Record [partial A3 preparation](../evidence/dev-147-t1-staging-preparation-hold-2026-08-30.md), not a completed package or goal. Preserve the original three-assertion RED, rejected unexecuted v1, accepted two-assertion privacy RED and actual seven-method sync-free run. Independent read-only result QA passed without a rerun. Keep full 54-method GREEN on HOLD: adding only the historically pinned sync to unchanged v5 needs separate approval because sync -f flushes the containing filesystem. The active fixed-profile capture/binding implementation is still missing; private recovery and one-case drafts are not release instructions. No live/manual hold is relaxed.

- 2026-08-30: Accept the [sole private T1 assembly](../evidence/dev-147-t1-private-image-2026-08-30.md), `run-mvqmtbw_`, after independent pre-execution review and fresh probe `run-vpqm7rk3`. Independent read-only result QA passes with zero failures. Exactly one TIPD payload changes; seven indexes and 1,162 other main raw records remain identical. This supersedes the pre-execution status below without rewriting that history. Move to offline A3 handoff-package preparation/review, then A4 sealing. No retry, staging, load, boot, hardware or manual action is released.

- 2026-08-30: Independent T1 assembly QA `run-zbpyg548` passes 3/3 with outer exit 0, no timeout, all 595 fingerprints unchanged and zero child/image/result output. Actual assembly remains stopped for final pre-execution review and a fresh exact-input v5 probe. Complete result validation and independent candidate/readback audit remain mandatory; final-path presence alone is never PASS.

- 2026-08-30: Record the [fixed T1 assembly source/fixture checkpoint](../evidence/dev-147-t1-assembly-preexecution-2026-08-30.md) after contained offline authorization. Preserve exact RED snapshot `852a03...` and GREEN `run-_6v39awz`, 3/3, zero children, unchanged inputs, no image. The existing pure archive contract stays unchanged; the dedicated assembler binds accepted real module/proof bytes. Actual assembly remains stopped for independent QA/review and a fresh exact-nine-input v5 probe. This supersedes the earlier authority HOLD only for contained offline work. Every live/manual hold remains active.

- 2026-08-30: Accept [third E-control attempt](../evidence/dev-147-e-operational-third-attempt-pass-2026-08-30.md) `run-988kuwr1` as the first fresh operational E no-change PASS. Fresh probe `run-y3xcwg2_` passed. All 424 children and 1,272 child files validate. The final header, evidence, and result bind exact reconstruction, lookup, index, containment, and unchanged-input facts. Independent read-only QA found zero failures. Final documentation review found no blocker. No retry, candidate binding, image, load, stage, boot, or manual action occurred. Preserve both earlier fail-closed attempts. T1 assembly is now the next separate offline review gate and remains on HOLD pending explicit authority.

- 2026-08-29: Accept the [private T1 module candidate checkpoint](../evidence/dev-147-t1-private-candidate-2026-08-29.md). Two contained builds produce byte-identical candidate modules. The corrected independent inspection passes with 592 read-only bindings, 99 strong imports, 9 exports, and unchanged scoped layouts. Preserve the first stale-import-list exit 1 as history. The source and module remain private and were never E-control inputs. No image, load, stage, boot, or hardware result follows. At that checkpoint, hold T1 assembly until a fresh E-control PASS.

- 2026-08-30: Record [second E-control attempt](../evidence/dev-147-e-operational-second-attempt-lookup-grammar-2026-08-30.md) `run-noq24xg7` as fail-closed, not PASS. All 424 children completed. Gzip `-n` matched retained E. Validation stopped at the first module lookup because actual kmod 34.2 `insmod` lines have exactly one trailing ASCII space. Preserve controlled helper RED and accept the narrow SWE GREEN: 18/18 helper fixtures and 25/25 zero-production-child boundary methods. [Independent QA](../evidence/dev-147-e-lookup-correction-independent-qa-2026-08-30.md) repeated both sets at 18/18 and 25/25. Final review passed with no blockers. No retry, fresh proof, T1 image, or manual action followed from that result. At that checkpoint, any third attempt needed a separate GO and fresh v5 probe.

- 2026-08-29: Accept the zero-child E execution boundary after exact RED, retained first-GREEN stop, narrow path-subclass correction, corrected 3/3 GREEN, and independent QA/safety review. Record zero children, unchanged inputs, 594 test mounts, the fixed eight-input/593-mount production model, and no operational artifacts. Keep the real workload and all operational APIs closed pending a separate pre-execution review. This checkpoint does not implement T1 or authorize image, staging, sudo, reboot, cable, device, or recovery work.

- 2026-08-29: Record the read-only A0 HPD/source and saved-log comparison. After independent safety and four-document QA PASS plus source/design review, select T1 only for offline A2–A4: bounded TIPD sender diagnostics with unchanged hardware operations and E-equivalent packaged-DWC3 availability. The core-local allocation tail preserves shared layouts and the frontend; the exact CD321x variant guard precedes tail access. Preserve the initial proposal anchor, historical DWC3/ATC contract, all prior results, and USB/full Gate 4b HOLD. At this selection checkpoint, no T1 source, build, or image existed. The later [private candidate checkpoint](../evidence/dev-147-t1-private-candidate-2026-08-29.md) supersedes only that availability statement. No behavior fix, manual action, or test ladder is approved.

- 2026-08-28: Add A0–A4 for the authorized autonomous offline goal: pin/compare saved evidence and sources, independently select one justified candidate or diagnostic, implement/test/build in fresh containment, prepare the staging/recovery handoff, then QA/review/seal/publish/sync. Alternatively, independently substantiate a NO-GO dossier with the indispensable missing observation/authority and minimal next action; do not substitute uncertainty or failing tests for that proof. Stop before manual action. Retain E/D3 failures, latest W attribution limits, firmware/USB HOLD, and the main plan's separate user-attested charging report. Greeter work and live tests remain excluded.

- 2026-08-28: Consume the intended W handoff after observed functional display recovery. Fresh boot, native outputs, readable pins, and the bounded kernel capture agree; the user did not restate the filename, so shared IDs alone do not prove W artifact startup. Keep USB/full Gate 4b HOLD and E/D3 causality unresolved. Record the login-focus observation separately, without claiming a primary-monitor switch or reproduced authentication bug. Any greeter fix and disposable tests require separate approval; no further device action follows.

- 2026-08-28: Resolve the selection hold with David's E confirmation. Together with the saved boot capture, this records E external-display FAIL without proving cause. Fresh readiness checks are on the same boot; no new full-journal or USB capture was made. Prepare the already-reviewed one-use W recovery for final release. Preserve previous evidence and historical procedures; W remains unperformed, E consumed, USB/full Gate 4b HOLD, and no test ladder authorized.

- 2026-08-28: Preserve the new post-reboot display-loss record without assigning it to E. A new boot is verified, but W/E share loaded IDs and the selected filename is unconfirmed. Retain the 1,012-record envelope, scan-end cursor caveat, ordinary firmware diagnostics, and audit suppression. Consume the intended E handoff and hold for exact selection; W recovery remains conditional and unreleased. No retry, cable action, diagnostic-trace failure claim, or new causality result.

- 2026-08-28: Supersede the unknown physical-setup hold with user confirmation and matching read-only checks. Independent readiness QA and exact E/W handoff safety review pass; David confirms saved work and the Mac guide on another device, and asks to reboot when ready. Main plan owns one conditional E selection and separate one-use W recovery; final task release, new E/W access/read-through outside Linux, and choosing the exact case remain required. Clarify recovery filename/cancel/missed-edit handling and usable-screen fallback before use. Preserve zero-marker expectations, loaded-ID ambiguity, safety-first capture limits, USB-only HOLD, all prior evidence, and no E boot claim or B/G ladder.

- 2026-08-28: Record C3 user-run staging PASS and qualified receipt QA. All 41 visible rows are exact initial-preflight output; the final report evidences later root-private checks/marker/sync, without a separate numeric exit capture. Independent 37-row hashes, source identities, seal, and E metadata agree. Preserve the historical offline-preparation section and consume its pending command. E is staged but unbooted; hold C4 for fresh physical/readiness confirmation and a separately reviewed one-time selection/recovery proposal. No replay, duplicate privileged read, or new hardware claim.

- 2026-08-28: Complete C3 offline E-only helper preparation after the C2 seal. Retain two genuine old-image/proof RED assertions; syntax, 42 focused methods, independent saved-run QA, helper review, and exact three-assignment private-copy review pass. Preserve all 38 old test bodies, all 32 old protected rows, and historical artifacts. The 33 + 8 fixed producer records do not prove root preflight. Hand off one staging-only command to David; E remains unstaged/unbooted and C4 remains separate. No monitor connection is required for staging and no cable change or retry is authorized.

- 2026-08-28: Record C2 offline checks and independent QA PASS. Preserve semantic metadata/E REDs and the expected old-v1 verifier rejection before separate GREEN results. Fresh controls and strict-v2 modules are verified; assemble only E with packaged DWC3 and unchanged ATC/TIPD/unrelated records. Retain exact artifact identities and all failures in dated evidence. B/G images remain unprepared. After checkpoint review/sealing, continue with a new E-only C3 staging helper and reviewed manual handoff; no privileged staging, image selection, or device action follows automatically.

- 2026-08-28: David explicitly approved continued work until monitor connection, sudo, or reboot needs his support. Proceed with C2 contained module/control-image preparation, focused QA, and independent review, then prepare the next exact manual handoff. C2 results remain pending. Keep old artifacts and W/E/B/G controls intact, stop on failed gates, and do not stage or boot automatically. This approval does not authorize external messages or submissions.

- 2026-08-28: Complete separately approved C1 source correction and focused userspace tests. Preserve genuine two-target generation-zero RED before producer changes. Apply the exact OF path/reference guard and strict v2 identity; correct the initial null-node boundary during static review before v2 testing. All 10 target methods / 54 fixture executions, 65 trace tests, and 57-site format/cap checks pass, with independent QA and source/test review. Retain C0/D3/recovery evidence and historical v1 patch/schema/build/binary helpers. Next approval is C2 preparation only; no hardware result, video-cause claim, or acceptance closure follows.

- 2026-08-28: Complete independent C0 design review in this existing subplan. Prefer fixed absolute OF-path lookup with non-null pointer identity and balanced references, not raw ancestor walking under dynamic/overlay OF support. Disclose the narrow metadata-lock/refcount timing addition. Require actual production generation-to-marker RED/GREEN, real FDT naming semantics, strict `dev147-usbdiag2-v1` identity, and preserved v1 negatives. Define W/E/B/G controls without a boot ladder; E is only a preferred later single proposal. Next approval is C1 source/tests only, with module/image preparation, staging, and any attended boot separately gated. No source or hardware state changes in C0.

- 2026-08-28: Record one functional working-image recovery PASS after both physical screens and loaded identities validate. Keep D3 external-display FAIL / measurement INCONCLUSIVE, its unfixed logging guard, and the unknown video cause. Only root hubs are present, so USB acceptance stays HOLD. Supersede the pending recovery handoff without deleting its steps or earlier evidence. Next is a separately reviewable offline correction-design proposal; no implementation, new test, or hardware action follows from recovery.

- 2026-08-28: D3 failed the external-display criterion while the internal screen remained usable. All intended loaded IDs match, but the 963-record private capture has zero markers. Its journal envelopes validate; diagnostic measurement completeness does not. Prove the leaf-name/absolute-path mismatch from exact pinned OF sources and matching driver sources. Keep video causality unknown, retain all earlier PASS/failure history, and record the missed semantic coverage. The D3 handoff is consumed; do not repeat it.
- 2026-08-28: Use the main plan as the sole current recovery handoff: one user-selected boot of the previously working DP image, with unchanged cables and kernel arguments. Result pending; not full DTB rollback. Only after physical recovery and read-only identity validation, scope an offline guard-regression design and examine earlier DWC3 availability. No fix, build, new diagnostic boot, reconnect, live swap, or suspend is authorized.

- 2026-08-28: David cleared the physical readiness hold: MagSafe/front-lower monitor connected, no other active work, both screens normal, and no downstream USB device. Fresh preflight passes, with protected root-only bytes still evidenced by his D2 validator. Save one explicit desktop-to-GRUB handoff using `initramfs-linux-asahi-dpalt-usbdiag1.img`; keep kernel arguments, normal boot, cables, and modes unchanged. One attended restart only. No diagnostic startup or USB result has occurred at this checkpoint.

- 2026-08-28: D2 user-run staging PASS. The complete transcript matches all 32 protected and eight proof records. The root-owned mode-0600 diagnostic image is 19,647,739 bytes; the check directory is root-owned mode 0700. Accept the helper's protected post-checks from David's validated execution, not an independent root-log or image-byte read. Preserve sealed evidence and the unselected image. D3 requires fresh physical readiness and explicit approval; no boot is authorized.

- 2026-08-28: D2 preparation passes final independent review and all 38 isolated tests. Preserve genuine RED, the missing-`/dev/fd` correction, and the EXIT-trap local-scope defect and regressions. The final private copy differs in exactly three fixed assignments; its production preflight has not run. Hand off staging only, preserve all existing images and evidence, and keep D3 separately gated.
- 2026-08-28: David approved plan reconciliation and the recommended D2 preparation. Implement and test a new staging-only helper without changing the image, old operational helpers, live checkout, boot files, packages, or drivers. Preserve the frozen D1 archive and use fresh private outputs. Stage only through David's reviewed command; keep D3 and all release/upstream actions separate.

- 2026-08-28: Design observations in the two device drivers, not a behavior correction. Reason: source order is established, but this boot's actual sequence and hardware causality are not.
- 2026-08-28: Record all first-probe and automatic retry generations, plus null/error booleans. Reason: a later `PROBE_PENDING` attempt can already hold a PHY or error pointer.
- 2026-08-28: Retain bounded native INFO/JSON logging without deferred printk or live tracing controls. Reason: the exact module export boundary and observer timing must remain explicit.
- 2026-08-28: Propose adding the missing DWC3 glue only to a distinct diagnostic image, with no explicit preload or runtime hook. Reason: both drivers must be observable at first probe; earlier availability is a known timing confound. Whole-image and round-trip review must pass before staging.
- 2026-08-28: Require a separate reduced index-generation root with pinned missing inputs and a no-change control. Preserve builtin indexes and archive boundaries. Reason: the working archive is not a complete depmod input tree; bare regeneration could alter unrelated boot content.
- 2026-08-28: Complete D0 after independent source/measurement, image/rollback, and safety review. Require an implementation revision at first probe and explicit handling of missing final records. Reason: a later module identity or consecutive prefix does not prove capture completeness. D1 remains subject to separate approval; D2 and D3 remain separate gates.
- 2026-08-28: David approved D1 with “yes, proceed” after the explicit offline implementation/build handoff. Start with pinned-input and actual sandbox checks. Keep all outputs in a fresh private directory. No installation, staging, live driver action, or reboot is authorized.
- 2026-08-28: Pause D1 after three production-sandbox QA failures. Retain the namespace assertion's incomplete evidence; do not call it either an escape or a pass. Save all source/fixture drafts, input pins, and failed runs. Ask before a narrow correction and one further isolation round. Do not build, stage, or boot while containment is unproved. The monitor may be disconnected during this offline hold; its current physical state was not asserted.
- 2026-08-28: David approved one narrow correction and one additional isolation QA round with “yes, go ahead!”. R4 passed after recording actual namespace results and accepting only the reviewed denial codes. Preserve the earlier three failures and their unknown errno. Resume the already approved offline D1 scope in fresh private outputs; implementation/build/image gates remain unfinished. This R4 run made no driver, image, or hardware change. D2 and D3 remain separately gated.
- 2026-08-28: Prepare the public source checkpoint on a clean branch, while preserving the original private branch and both frozen checkpoints. Publish authored drafts and fixtures with explicit unimplemented/unbuilt labels. Keep raw device/Linear records, tool manifests, package binaries, images, and backups private; public machine placeholders cannot substitute for the tested operational originals.
- 2026-08-28: The fresh private continuation passed the same isolation checks, then ran the frozen trace/image stubs once to establish RED. All reported errors are NotImplementedError; inputs stayed unchanged and neither run timed out. Proceed to implementation and independent QA, not staging or hardware testing. R4's historical result remains a separate record.
- 2026-08-28: Complete the source/build, import/logging, and real no-change controls. Preserve the newly observed archive format without normalizing its raw bytes. Allow only the verified header-only weakdep scratch file outside the image. The base image and seven indexes reproduce exactly; all 199 binary-only lookups pass. Use raw-record preservation and stdout-only selected extraction instead of general archive extraction. Proceed to private diagnostic assembly and delta QA. D2/D3 remain unauthorized.
- 2026-08-28: Complete offline D1. Retain the failed assembly and baseline alias-dump check. Prove that symbol-index drift is limited to ordering priorities, and preserve the original symbol bytes. Correct only kmod alias-key normalization; keep raw symbol keys and multiplicities. All 55 assembly fixtures and the real 413-command candidate run pass. The private image has exactly the approved four changes and all 200 binary-only lookups pass. Preserve the original fixture-order miss and genuine later RED runs. Next requires approval for D2 staging preparation; no staging, live load, reboot, or hardware acceptance is implied.
