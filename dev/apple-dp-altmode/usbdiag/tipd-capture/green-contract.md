# Fixed T1 capture — focused GREEN implementation

Status: authored, unexecuted, pending source review and a fresh exact-input probe. The preserved RED directory is unchanged: probe `run-x98n3klr`, then `run-gv3ttd_p` reached exactly three intended assertions, with no errors/skips or fixture children. This directory is a separate version, not a replacement of that evidence.

The exact proposed command is `/usr/bin/python3.14 -I -S -B /inputs/tests/run_fixed_capture_tests.py`. Inputs are this whole source directory at `/inputs/tests` and the unchanged synthetic expected prefix at `/inputs/staging-prefix`; the existing harness adds `/inputs/proof`. The unchanged approved v6 runtime supplies all dependencies. No launcher/runtime repin, installation, historical replay or live entry is part of this command.

The original three test methods remain byte-identical. Ten focused methods add subcases for both stream cap boundaries, concurrent streams, actual clean environment/null stdin/closed inherited descriptor/new session, nonzero child exit, silent/continuous/TERM-ignoring timeout, descendant-held pipes, real INT/TERM interruption, no-overwrite collisions, same-inode byte tampering, partial-open cleanup, one injected write error, bounded synthetic samples/replacements, fixed wrapper shape, no fixture-to-journal relabeling, consistency publication/interruption, strict attestations and incomplete traces. No accepted parser suite or staging suite is replayed.

Expected selection: 13 methods. The source contains 21 planned direct Python fixture child invocations, not production children or journalctl execution. Nineteen cases should retain valid execution-only `child.json` plus bounded raw stdout/stderr. Two intentional readback/publication failures retain partial files instead: one colliding `child.json` is the literal fixture text, and one tampered-stream case has no `child.json`. Their fixed fixture scripts report their own PID on stderr, and the test checks that each PID is gone. Those incomplete cases must not be counted as valid receipts. Expected direct fixture exit intents are 11 zero exits, one exit 7, and nine SIGKILL stops; actual observations remain to be audited after execution. A test-local ENOSPC seam is a synthetic write error, not a physical storage-failure claim.

`bounded_child._capture_child` is internal. It uses real subprocesses with actual argv, fixed clean environment, null stdin, closed other descriptors and a new process group. The absolute deadline reserves cleanup time inside the duration; drain and direct-child reap cannot receive a new unrestricted allowance. INT/TERM handling stays flag-only through spawn/pipe registration, then interrupts active drain and restores handlers afterward. Failure kills only the owned group and makes a bounded direct-child reap attempt. Unreaped, over-deadline, failed, capped or interrupted outcomes cannot be accepted as a clean collection. Counts describe observed and retained bytes, not an unknowable emitted total. Readback must match the bounded bytes actually written from pipes.

All test outputs are fresh private files under `/work`. Open/create is exclusive and no-follow. Failure preserves files and never retries or removes evidence. Synthetic consistency publication is not an execution receipt. The fixed live wrapper is never called by these tests, including its refusal paths; only internal synthetic file seams and static source checks are used.

The no-CLI `collect_fixed_t1()` entry is implemented but HELD and UNRUN. It has one fixed private output literal, exact boot-ID and module-note paths, the pinned journalctl binary and exact seven-argument command. Its whole collection deadline is 30 seconds, stdout cap is 8 MiB, stderr cap is 64 KiB, and cleanup reserves one second. The fixed output guard runs before live reads. A later public copy will replace only `LIVE_OUTPUT` with an invalid `LOCAL_ONLY` literal, which must fail before uname, journal or sample access. No runtime path, command, fixture or profile override is exposed.

The wrapper records startup as incomplete, reads before/after samples with bounded no-follow reads, checks bytes and metadata without treating proc's zero advertised size as zero payload, authenticates journalctl before/after and keeps actual execution separate. It derives the accepted consistency format only for the exact actual journal argv and a clean complete child result. A Python fixture result is refused. Parent sample/parse/publication errors retain a failure record when writing remains possible; the startup record remains if later failure recording also fails.

`capture-result.json` is explicitly provisional. Its presence never establishes completion. Acceptance requires a complete validated record, all raw-file/readback checks, no parent failure, and separately observed outer exit 0 within the deadline. An interruption or post-publication deadline failure can leave the provisional pathname and still fails the operation. There is no atomic-publication claim. All operational, initrd-boot, earliest-load, negative-sender, receiver and hardware acceptance claims remain false.

The separate pure binding uses the fixed image/module/helper identities, the exact 45-line prefix digest and three completion lines, strict staging JSON plus raw stdout/stderr and a separate exact filename/boot-ID selection attestation. Unknown staging exit or incomplete/limited trace remains inconclusive. A provisional staging `RESULT.txt` alone is insufficient. User attestations and submitted bytes are consistency only, not proof of staging or what booted. A complete-looking prefix cannot rule out an entirely absent closed suffix, and HPD return does not establish receiver delivery.

All common parser bytes and the original three tests remain unchanged. The explicit typed-stdlib/dataclass/unittest no-install exception remains. All live/journal/sysfs/power, sudo/staging, cable/device, reboot/recovery, runtime-expansion and upstream holds remain active. This is not a ready or sealed A3/A4 handoff.
